# 📑 [문서 0] 이터레이션 및 정합성 유지 가이드 (Alignment Constitution)

본 문서는 피그마(Figma) 자산 없이 마크다운 문서와 단일 `app.js` 코드만을 활용해 제품을 빌드할 때, 기획·디자인·개발 스펙 간의 얼라인먼트가 무너지는 현상(Spec Drift)을 원천 차단하기 위한 프로젝트 최상위 헌법입니다. 본 가이드의 규칙은 프로젝트 내 모든 문서 및 소스 코드에 강제 적용됩니다.

---

## 1. 역방향 추적 태그 시스템 (Reverse-Tracking Tags)

문서의 명세와 코드의 정합성을 유지하기 위해 **[고유 ID 태그]** 체계를 사용합니다. 코드와 문서 간의 싱크는 이 태그를 기준으로 양방향 추적됩니다.

1. **태그 구조:** `[분류-식별키]` (예: `[PRD-EX-01]`, `[FLOW-H01]`)
2. **코드 내 주석 의무화:** `app.js` 내부에 특정 기획 로직, 컴포넌트 구조, 데이터 이벤트를 구현할 때 반드시 해당 영역 최상단에 문서와 일치하는 주석 태그를 명시해야 합니다.
   * *예시:* `// [PRD-EX-01] Deadlock 완화 알고리즘 로직 시작`
3. **이터레이션 수정 규칙:** 
   * 기능 수정이나 UX 변경이 필요할 경우, 코드나 문서를 임의로 고치지 않습니다.
   * 변경하려는 영역의 [태그 ID]를 확인한 후, **01_master_prd.md의 해당 태그 구역을 먼저 수정 ➡️ 코드를 수정**하는 순서로만 이터레이션을 진행합니다. 태그가 유실되거나 매핑이 깨진 코드는 불완전한 스펙으로 간주하여 병합을 거부합니다.

---

## 2. 디자인 토큰의 중앙 집중 격리 (No Inline Classes)

디자인 이터레이션 시 발생할 수 있는 스타일 파편화를 막기 위해 `app.js` 컴포넌트 내부에 스타일 코드를 직접 심는 것을 금지합니다.

1. **인라인 스타일 금지:** 컴포넌트 마크업 내에 `className="bg-neutral-900 p-4 text-white"`와 같이 Tailwind CSS 클래스를 직접 하드코딩(Inline)하지 않습니다.
2. **토큰 변수 참조:** `app.js` 파일 최상단에 `const T = { ... }` 오브젝트를 선언하고, 02_design_system.md에서 정의한 토큰을 변수 형태로만 참조하여 UI를 조립합니다.
3. **효과:** 간격, 서체, 컬러 등 디자인 시스템 수정 요청이 발생하면 `app.js` 최상단의 토큰 변수 정의부 1곳과 02_design_system.md의 명세만 수정하면 프로젝트 전체에 즉시 반영됩니다.

※ 격리 의무 범위는 색·곡률이다. 타이포·간격은 Tailwind 표준 스케일 한정으로 인라인 사용을 허용한다 (02_design_system.md 1.1-2 참조).

---

## 3. 단방향 데이터 흐름 및 가상 상태 유지 규칙 (State Preservation)

서버가 없는 환경에서 가상 라우팅을 구현할 때, 화면 전환으로 인해 작성 중이던 임시 가용성 데이터가 증발하거나 정합성이 깨지는 문제를 차단합니다.

1. **원천 데이터 정의:** 모든 화면 컴포넌트는 `app.js` 최상단에 선언된 가짜 데이터 생성 함수(`function buildSeedData()`)의 스키마 구조를 그대로 상속받아야 합니다.
2. **상태 변경 시점 제약:** 유저의 인터랙션(예: 타임 슬롯 드래그) 중에는 React 로컬 상태(`useState`)만 변경하며, 최종 [제출/확정] 액션이 발생하는 순간에만 `localStorage` API를 통해 단방향으로 데이터를 동기화합니다.
3. **가상 라우팅 언마운트 방어:** 가상 URL 스위칭 시 상위 콘텍스트(`Context Provider` 또는 최상위 `state`)에 가용성 편집 임시 데이터를 격리 보관하여, 화면 복귀 시 수정 중이던 그리드 데이터가 그대로 유지되도록 구현해야 합니다.