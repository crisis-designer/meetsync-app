# 🧪 [문서 4] 프로토타입 테스트 가이드 v2.6

기준: PRD v3.0 / DS v2.9 / DEV v2.8. 15차 QA 발견 4건(87~90: 웹훅 이탈 특별취급 폐지, 회의 후보 기간 최대 7일 정책 + 배경 유입 시드 근본 수정, CONFLICT 통합 플로우(취소=대안선택 강제 + 원클릭 재확인), 원클릭 재확인 버튼 비활성화) 반영. 16차 QA 발견 1건(91: 반응형 감사 — A01 그리드 요일 컬럼이 후보 기간 최대 7일에서 44px 터치 타겟 미달, 부수적으로 AB01·P01 닫기 버튼도 발견 52 터치 타겟 원칙의 예외였음을 확인)도 이번 회차에 반영한다. 17차 QA 발견 1건(92: 코드 품질 감사 — 화면 16개가 App() 내부 클로저·일반 함수 호출이라 실제 컴포넌트가 아니었던 구조를 AppContext + 실제 함수 컴포넌트로 리팩터, DEV 3.0)도 반영한다. 18차 QA 발견 1건(93: index.html에 Tailwind CDN 스크립트를 수동으로 넣어야만 디자인 시스템이 동작하던 것을 app.js 자체 부트스트랩으로 전환, Geist 폰트 CSS 경로가 실제로는 404였던 것을 실존 .woff2 직접 참조로 수정)도 반영한다. 19차 QA 발견 1건(94: 실무자 셀프서브 감사 — 00/02/03 문서가 실제 코드에 없는 변수명(DESIGN_TOKENS·SEED_MEETING_DATA)을 안내하고 있어 문서만 보고 따라가면 코드에서 못 찾던 문제, 히트맵 0인원 셀의 하드코딩 hex 토큰 이탈, stale 고정 줄번호 주석을 정정)도 반영한다. 이전 회차(T0~T36) 시나리오는 유지되며, 아래 T37이 신규 항목이다.

> **사전 검증:** esbuild 통과, 금지 항목(얼럿·리로드·실제 시계) 0건 — 이번 회차에서 재확인. 발견 87~90(경로 통합, 후보 기간 7일, buildMemberResponse, 게이팅) 대상 신규 타겟 회귀 테스트 **14종을 이번 회차에서 직접 실행해 전건 통과 확인**(calculateBestTime level 판정, buildMemberResponse 기간 커버리지, deriveRematchGate 경로 무관 통일 판정, trackRematchUpdate). 다만 기존에 문서에 기록되어 있던 "로직 자동 테스트 54종"은 원본 테스트 파일을 전달받은 적이 없어 이번 회차에서 재실행하지 못했다 — 그 54종 스위트는 별도로 보유하고 계시다면 함께 재통과 확인이 필요하다.

---

## 1. 세팅

`src/App.jsx` → 새 app.js 교체 (Tailwind CDN 유지) → `Ctrl+Shift+D` → **[데이터 초기화]** 필수 (시드 구조 변경).

---

## 2. 시나리오

### T0 — 미발의 가드 (신규)
| 단계 | 기대 |
|---|---|
| 초기화 직후(미발의) 패널로 D01·A01 점프 | **H01로 리다이렉트** — 링크 카드도 유입도 없는 유령 화면 없음 |
| L01·가이드·H01 진입 | 정상 |

### T1 — 해피 패스 (발의 → 유입 → 확정 → 순환)
| 단계 | 기대 |
|---|---|
| H01: 멤버 권한 토글 조작 | **발의 전에만 가능** |
| [회의 개설...] | 발의 커밋 → D01. **다시 H01 방문 시 전원 잠긴 뱃지("… · 잠김")** |
| D01 도착 | 0/6 → 1초 간격 유입 5/6. **참석자 화면으로 이동해도 유입 계속** (백그라운드) |
| A01 그리드 | 탭 순환 가능→피하고 싶음→불가→해제. 잠금 슬롯 탭 시 **실제 일정명 토스트** + 하단 상시 출처 캡션 |
| 제출 → D01 → Top 3 | 카드의 [최종 확정] 1탭 | **카드 하단이 확인 영역으로 확장** (armed 버튼 아님) — [취소]/[이 시간으로 확정] |
| 확정 2탭 | COMPLETED. **확정 카드가 최상단 히어로**, 히트맵은 "조율 근거 다시 보기"(접힘), 확정 슬롯에 **굵은 링 마커** |
| 히트맵 임의 슬롯 탐색 클릭 | **얇은 회색 링** — 확정 마커와 구분됨 |
| 패널로 A01 | 확정 안내 + [갑자기 참석이 어려워졌나요?] 링크 |
| D01 [새 회의 만들기] | 확인 영역(문장형) → 실행 → L01 |

### T2 — 불변식 검증 (핵심 — 신규)
| 단계 | 기대 |
|---|---|
| 박개발로 불가만 잔뜩 지정해 부분 성립(1명 불참) 유도 | 카드에 **[최종 확정] 버튼 자체가 없음** — 불변식(COMPLETED는 항상 필수 전원 참석) |
| 대신 노출되는 액션 | **[가용성 재요청]**(주) + **[{이름}님을 참조자로 변경하고 진행]**(보조 텍스트 링크) |
| [가용성 재요청] | 발송 → 버튼이 **"재요청 보냄 ✓"** 비활성 톤으로 전환 (무상태 아님) |
| 재요청 대상으로 재진입 → 해당 슬롯 개방 → 재제출 | 부분 성립 카드가 **상위 완화 단계로 즉시 대체** — 해소 루프 실동작 |
| (초기화 후 재현) [참조자로 변경하고 진행] 클릭 | 확인 영역: 재요청 미발송 시 경고+[재요청 먼저 보내기], 결과 문장(별도 공유 필요 명시) → [참조자로 변경] |
| 실행 후 | 대상이 참조자로 변경 + 추천 즉시 재연산 → 해당 슬롯이 "참조자 제외" 계열로 승격 → **이제 [최종 확정] 노출** |
| 필수 2명 이상 불참 카드 | 강등 제안 없음 — "필수 참석자 2명 이상이 불가한 시간입니다" 안내만 |
| 주최자(김주최) 본인이 병목인 카드 | "주최자 본인의 가용 시간을 조정해야 합니다" — 재요청·강등 미노출 |

### T3 — 강제 마감 확인·통지 (신규)
| 단계 | 기대 |
|---|---|
| 박개발 미제출 + 시계 '마감 도달' | 경고 배너. [제외하고 강제 마감] 1탭 | **확인 영역** 확장 — 독촉 미발송이면 경고+[먼저 독촉 보내기] |
| [먼저 독촉 보내기] | 발송 → 이력이 "독촉 발송함 ✓"로 갱신 |
| [제외하고 마감] | 실행 → 5인 기준 결과. 현황 칩에 박개발 **"제외"** 표기 |
| 박개발 링크 재진입(PROGRESS 중) | 배너 "응답 마감으로 제외되어 있습니다. 지금 제출하면 즉시 반영됩니다" |
| 제출 후 | 자동 재포함, 배너 소멸 |

### T4 — 참석 취소 선언 (경로 A, 신규 실구현)
| 단계 | 기대 |
|---|---|
| 해피 패스로 확정 완료 | A01 확정 안내 하단 [갑자기 참석이 어려워졌나요?] |
| 본인 선택 → 필수(예: 최개발) | 확인 영역: "회원님은 필수 참석자입니다... 재조율이 시작됩니다" + [돌아가기]/[참석 취소하기] |
| [참석 취소하기] | **CONFLICT 실전이** (연출 아님) + 주최자 지속 배너("최개발님이 참석을 취소했습니다") |
| 이탈자 본인이 A01 재진입 | **전용 안내**: "회원님의 참석 취소로 재조율이 진행 중입니다" — **가용성 업데이트 동선 없음** |
| 다른 참석자가 A01 재진입 | "재조율 진행 중" + **[내 가용 시간 업데이트하기]** → 재편집 → 재제출 → **R01 대체안 즉시 재연산** |
| 본인 선택 → 참조자(정마케) → [불참 알리기] | 상태 전이 없음, 토스트만. 확정 안내에 불참 표기 반영 |
| 패널 [이탈 발생시키기](경로 B) | COMPLETED에서만 활성 — 최개발 고정, 힌트에 "외부 캘린더 삭제 감지 시뮬레이션" 명시 |
| 발동 직후 최개발의 확정 슬롯 가용성 확인 (v2.3, 발견 87) | 자동으로 BLOCK_STRICT 마킹(사유 "외부 일정 변동 감지")되어 있는지 — 마킹 없이 상태값만 바뀌면 같은 슬롯이 재추천되는 결함 |
| 최개발 본인이 A01 재진입 (v2.3, 발견 87) | 더 이상 "회원님의 일정 이탈로 재조율이 진행 중입니다"(읽기 전용)가 아니라, 다른 참석자와 동일하게 "다른 시간을 다시 찾고 있어요" + [내 시간 다시 알려주기] → GridEditor 진입 가능한지 |

### T5 — EX-04 실변동 (배너 대상 일반화 포함)
| 단계 | 기대 |
|---|---|
| EX-04 ON → 확정 확인 → 2탭 | 확정 취소, 배너에 **대상 실명 + 제외 슬롯 특정**. Top3에 **"새 추천"** 표시 |
| (사전 조건) 박기획이 이미 강등된 상태에서 EX-04 발동 | 대상이 박기획이 아닌 **다른 적격 필수 응답자**로 자동 전환 — 배너가 거짓말하지 않음 |

### T6 — 히트맵 (절대 램프 검증)
| 단계 | 기대 |
|---|---|
| 가능 인원 4명 슬롯 vs 5명 슬롯 비교 | **육안으로 다른 농도** (이전 비율 밴드에서는 동일 색이었던 케이스) |
| 상세 패널 | "가능 {n}명" 수치 표기 + 5종 상태(가능/비선호/불가/외부 일정/미확인) 구분 |

### T7~T11 — 기존 유지 항목
EX-02 잠금(출처 캡션·실명 토스트) / 멀티탭 동기화(유입 포함) / 가이드 스캔 구조(단계 카드·kbd·3단 블록) — 04 v1.3 대비 변경 없음, 재확인만.

---

## 3. 합격 기준

기존 항목 + **불변식 위반(부분 성립 카드에 확정 버튼 노출) 0건**, **armed 버튼 패턴(자동 소멸 확인) 0건 — 전부 확인 영역으로 대체**, 배제 3경로 모두 확인 영역·통지 이력·사후 가시성 3요소 충족.

### T8 — 참석 취소 후 재조율 참여 (경로 A 재설계, 신규 — 발견 38·39)
| 단계 | 기대 |
|---|---|
| 해피 패스로 확정 완료 → A01 [갑자기 참석이 어려워졌나요?] → 필수 참석자(예: 이디자) 선택 → [참석 취소하기] | CONFLICT 전이. **취소자 본인은 연산에서 완전히 빠지지 않는다** — 확정됐던 그 슬롯 하나만 본인 기준 "안 되는 시간"으로 막힘 |
| R01 대체안 확인 | 이디자가 **다른 슬롯에서는 정상적으로 후보에 포함**됨 (이전처럼 이름 자체가 통째로 빠지지 않음) |
| 이디자 본인이 A01 재진입 | 다른 참석자와 **동일하게 "다른 시간을 다시 찾고 있어요" + [내 시간 다시 알려주기]** 제공 — 이전 버전의 "안내만, 동선 없음" 폐기 확인 |
| [내 시간 다시 알려주기] → 그리드에서 다른 시간 추가 지정 → 제출 | R01 대체안이 재연산에 즉시 반영 |
| 대체안 확정 | 완료 화면의 등록 안내와 히트맵 상세가 **일치**해야 함 — "불참 예상"이 실제로는 가능한 사람으로 표기되는 모순(발견 38) 재현 여부 확인 |
| **비교: 패널 [이탈 발생시키기](경로 B)** 로 최개발 이탈 발생 (v2.3부터 기대 결과 변경) | 최개발도 경로 A와 **동일하게** 연산에 포함되고 본인 재진입 시 업데이트 동선이 제공되는지 확인 — 유일한 차이는 반영 현황: 경로 A는 취소 선언 즉시 "✓ 갱신함"이지만 경로 B는 본인이 실제로 그리드 재제출을 해야 "✓ 갱신함"으로 바뀌는지 (T28 참조) |

### T9 — 히트맵 색상 위계 (신규 — 발견 40·41)
| 단계 | 기대 |
|---|---|
| D01 "다 같이 되는 시간 보기" 펼침 | 가능 인원 1~3명 구간이 이전보다 **뚜렷하게 명도 차이**가 남 (저농도 구간 뭉침 해소) |
| COMPLETED 화면의 "다른 시간대 비교해보기" | **확정 슬롯이 후보 램프와 다른 색 계열**(주최자 강조색 배경)로 즉시 눈에 띔 — 링 마커만으로 구분하던 이전과 비교 |
| 확정 슬롯 클릭 | "확정된 시간" 라벨 + 굵은 링 유지 |

### T10 — 배너 노출 범위 + 문구 전수 (신규 — 발견 42·43)
| 단계 | 기대 |
|---|---|
| EX-04 발동 또는 이탈 발생 상태에서 A01 진입 | 상단에 **지속 배너가 뜨지 않음** — 참석자는 자기 화면의 전용 안내만 봄 |
| 같은 상태에서 D01·R01 진입 | 지속 배너 정상 노출 |
| 전체 화면 훑기 (L01~R01, 패널 제외) | "가용성", "의사결정자", "참조자", "부분 성립", "응답 현황" 등 조직/개발 용어가 **화면에 노출되지 않음** — 전부 "내 시간", "필수 참석자", "선택 참석자" 등 일상어로 대체됐는지 확인 |
| 데모 가이드(GD01)만 예외적으로 기술 용어(EX-01, 완화 단계 등) 유지 확인 | 심사자용이므로 정확 지칭 유지가 맞음 |

---

## 3. 합격 기준 (갱신)

기존 기준 + **SELF_CANCEL 경로에서 취소자가 재조율 후보 연산에 계속 포함되는지**, **경로 A/B 화면 분기가 정확한지**, **호스트 화면 외 배너 노출 0건**, **사용자 노출 문구에 기획/개발 용어 잔존 0건**.

### T11 — 정렬 우선순위 역전 (신규 — 발견 49)
| 단계 | 기대 |
|---|---|
| 완화 2단계 이하로 후보가 여러 개 나오도록 데이터 조작 (예: 박개발로 특정 시간만 지정 제출) | Top3 정렬이 **"필수 참석자가 덜 불편한 시간"을 최우선**으로 뽑는지 확인 — 선택 참석자가 많이 오지만 필수 참석자가 피하고 싶어하는 시간이, 선택 참석자는 없어도 필수 참석자 전원 괜찮은 시간보다 하위에 있어야 함 |
| 카드 서브라인 확인 | "피하고 싶은 시간대: {필수 참석자 이름}"이 실제로 적은 인원 순으로 상위에 배치되는지 |

### T12 — 강제 마감 사유 3분기 (신규 — 발견 44·48)
| 단계 | 기대 |
|---|---|
| 박개발 미제출 상태로 [빼고 마감하기] 실행 후 확정 | 확정 확인 문장에 **"박개발님은 답이 없어 포함되지 않았습니다"**가 추가로 표기되는지 (기존엔 "전원이 참석 가능한 시간입니다"만 떴음) |
| 완료 화면 확인 | "미확인 (마감까지 답 없었음): 박개발" — 예전 "불참 예상"이라는 뭉뚱그린 문구가 아님 |
| (비교) 참조자가 [불참 알리기]로 자발적 불참 | 완료 화면에 "불참 알림: {이름}"으로 별도 표기 — 강제 마감 사유와 문구가 다름을 확인 |
| (비교) 부분 성립으로 확정된 필수 참석자 불가 | "참석 못 함: {이름}" — 세 사유가 서로 다른 문구로 분리되어 있는지 |

### T13 — 히트맵 비균등 스케일 + 확정 슬롯 색상 (신규 — 발견 45·50)
| 단계 | 기대 |
|---|---|
| 히트맵 펼침 | 4명·5명·6명 가능 슬롯이 육안으로 뚜렷이 구분됨 (1~3명 구간은 압축되어 있어도 무방) |
| 확정 완료 후 "다른 시간대 비교해보기" | **확정 슬롯 배경이 실제로 칠해져 있는지** — 이전엔 `bg-primary` 리터럴 버그로 흰 빈 칸처럼 보였음(발견 45). 진한 배경 + 링 마커 둘 다 확인 |

### T14 — 독촉 당사자 가시성 + 반응형 (신규 — 발견 51~55)
| 단계 | 기대 |
|---|---|
| 박개발 미제출 + [다시 알림 보내기] 실행 | 박개발로 A01 재진입(그리드 화면) 시 **"주최자님이 빨리 답해달라고 알려왔어요"** 배너가 뜨는지 |
| 브라우저 창을 360~390px로 좁혀서 확인 | 그리드 슬롯·히트맵 셀이 이전보다 커져 탭하기 편한지, 패널을 열었을 때 화면 절반 이상을 가리지 않고 내부 스크롤이 생기는지, 브랜드 바 역할 뱃지가 잘리지 않고 말줄임 처리되는지 |
| 그리드 화면 상단 확인 | 마감일만 헤더에 남고, 탭 조작법 설명은 하단 범례 옆 작은 텍스트로 이동했는지 (발견 46 위계 개선) |

---

## 3. 합격 기준 (갱신)

기존 기준 + **확정 슬롯 배경 렌더링 정상(투명 아님)**, **정렬 1순위가 필수 참석자 비선호 최소화인지**, **불참 사유 3종이 문구로 구분되는지**, **독촉 당사자 배너 노출**, **360px 폭에서 핵심 화면 레이아웃 붕괴 없음**.

### T15 — 뒤늦은 제출 경로 (신규 — 발견 60)
| 단계 | 기대 |
|---|---|
| 박개발 미제출 상태로 강제 마감 → 확정 완료 | 박개발이 A01 재진입 → [내 참석 상황을 확인하거나 바꾸고 싶어요] → 본인 선택 목록에서 박개발이 **"아직 답 안 함 · 지금 제출"**로 표시(다른 사람과 다른 라벨) |
| 박개발 선택 | **취소 플로우가 아니라 그리드 편집 화면**으로 직행 (경로 A와 분리 확인) |
| 확정 슬롯이 포함되도록 제출 | 상태 전이 없이 "이 시간에 참석 가능하신 걸로 기록했어요" 토스트만 뜨고 조용히 완료 |
| (재현) 확정 슬롯이 안 되게 제출 | **CONFLICT 전이** — 주최자 배너와 재조율 화면 문구가 "취소했습니다"가 아니라 **"뒤늦게 응답했는데 이 시간이 맞지 않아"**로 뜨는지 확인 |

### T16 — EX-04 확정 실패 슬롯 배제 (신규 — 발견 61)
| 단계 | 기대 |
|---|---|
| EX-04 ON → 특정 슬롯 확정 시도 → 충돌 | 그 슬롯이 Top3에서 **즉시 사라짐** — 같은 슬롯에 재차 확정을 시도해도 다시 나타나지 않는지 확인 (이전엔 간접 로직 의존으로 재등장하는 결함이 있었음) |
| [데이터 초기화] | 실패 슬롯 목록도 초기화되어 새 세션에서 이전 실패 이력이 남지 않는지 |

### T17 — 히트맵 색상 통일 + 강등 사유 (신규 — 발견 58·62·64)
| 단계 | 기대 |
|---|---|
| 서로 다른 시점에 히트맵을 두 번 열어 비교 | 같은 인원수라면 항상 같은 색 — 6명 슬롯이 유독 튀는 검정에 가까운 색이 아닌지 확인 |
| 부분 성립 카드에서 [선택 참석자로 바꾸기] 실행 | 강등된 당사자가 재진입 시 배너에 **"{슬롯}에 참석이 어려우셔서"**처럼 구체적 근거가 포함되는지 (이전엔 이유 없이 통보만 됐음) |

---

## 3. 합격 기준 (갱신)

기존 기준 + **미확인자 재진입이 항상 취소 플로우가 아닌 제출 플로우로 안내되는지**, **EX-04로 실패한 슬롯이 같은 세션에서 재등장하지 않는지**, **히트맵 색상이 문맥과 무관하게 일관적인지**, **강등·CONFLICT 배너에 사유가 실명·슬롯과 함께 명시되는지**.

### T18 — 회의 폐기 (신규 — PRD 5.6)
| 단계 | 기대 |
|---|---|
| D01에서 [이 회의 취소하기] → 사유 입력(선택) → 확인 | CANCELLED 전이. 대시보드가 즉시 읽기 전용 종료 화면으로 바뀌는지 |
| A01·다른 참석자로 재진입 | 상태 무관하게 "이 회의는 취소되었습니다" + 입력한 사유가 보이는지 |
| PROGRESS·COMPLETED·CONFLICT 각 상태에서 시도 | 어느 상태에서든 폐기 가능한지, 폐기 후 어떤 액션(재제출·재조율)도 열리지 않는지 |

### T19 — 역강등 요청 (신규 — PRD 5.1 [PRD-PROMOTE-REQUEST])
| 단계 | 기대 |
|---|---|
| 필수 참석자로 그리드 화면 진입 → [저는 꼭 필요한 사람이 아닌 것 같아요] → 요청 보내기 | 본인 화면에 "요청 대기 중" 배너, 주최자 대시보드 최상단에 통합 행동 배너로 승인/거절 버튼 노출 |
| 주최자 [승인] | 기존 강등 경로와 동일하게 즉시 참조자 전환 + 추천 재연산 |
| (재현) 대기 중 [요청 취소] | 요청 소멸, 배너 사라짐 |
| (재현) 요청 대기 중 주최자가 다른 슬롯을 그냥 확정해버림 | 확정 성공 시 요청이 자동 폐기되는지 — 재진입해도 잔여 요청 상태 없음 |
| 주최자 본인 화면 | [저는 꼭 필요한 사람이 아닌 것 같아요] 버튼이 안 보이는지 (권한 고정과 정합) |

### T20 — 강등 의견 남기기 + 기간 확장 (신규 — PRD 5.1)
| 단계 | 기대 |
|---|---|
| 부분 성립 카드에서 강등 실행 시 의견 입력 | 입력한 의견이 확정 완료 카드에 "{이름}님의 의견: …"로 노출되는지 |
| 부분 성립 카드의 [기간 넓혀서 다시 찾기] | 조율 기간이 늘어나고, 그리드·히트맵 컬럼이 5일로 확장되는지, 재계산된 추천이 실제로 달라지는지 |

### T21 — 프로액티브 재요청 + 일 단위 일괄 (신규 — PRD 5.3, 3.3)
| 단계 | 기대 |
|---|---|
| 본인 응답이 유일한 병목이 되도록 제출 시도 (예: 다들 가능한 슬롯에 본인만 비선호) | 제출 직전 "이 응답대로면 …만 빼면 전원 합의가 가능해요" 팝업이 본인에게만 뜨는지 |
| [네, 다시 볼게요] | 그리드로 복귀, 해당 슬롯이 하이라이트되는지 |
| [아니요, 그대로 제출] | 정상 제출 완료로 진행 |
| 그리드에서 날짜 헤더 탭 | "이 날은 종일 안 되는 걸로 표시했어요" + 해당 날짜 전 슬롯이 안 되는 시간으로 (기존 지정값은 보존) |

### T22 — 발의 후 제목 잠금 + 표시 문구 순화 (신규)
| 단계 | 기대 |
|---|---|
| H01에서 발의 후 재진입 | 제목 입력란이 읽기 전용 표시로 바뀌었는지 (권한 토글과 동일 잠금) |
| 히트맵 상세·완료 화면 등 타인이 보는 화면 | "불가"·"안 되는 시간" 같은 문구가 "다른 일정이 있어요" 류로 순화되어 있는지, 사유 3분기(미확인/참석못함/불참알림)는 여전히 정확히 구분되는지 |

---

## 3. 합격 기준 (갱신)

기존 기준 + **CANCELLED가 모든 상태 판정보다 우선하는지**, **역강등 요청의 확정 시 자동 폐기가 실제로 동작하는지**, **주최자 본인에게 역강등 요청 버튼이 노출되지 않는지**, **기간 확장이 실제 슬롯 풀을 넓히고 재계산에 반영되는지**, **프로액티브 팝업이 본인 화면에만 노출되는지**.

### T23 — 기간 확장 전원 통지 (신규 — 발견 79)
| 단계 | 기대 |
|---|---|
| 부분 성립 카드에서 [기간 넓혀서 다시 찾기] 실행 | candidatePeriod가 늘어남 + 주최자에게는 경량 토스트만(지속 배너 아님) |
| 이미 제출했던 참석자(예: 이디자)가 A01 그리드 재진입 | **기존에 입력했던 날짜의 응답은 그대로 유지**되고, **새로 추가된 날짜에만** "새로 추가됨" 강조 표시가 붙는지 |
| [재요청]과의 구분 확인 | 기간 확장 실행이 특정 멤버의 `reRequestedIds`를 건드리지 않는지 — 재요청 버튼 상태("재요청 보냄 ✓")가 기간 확장 실행 전후로 변하지 않아야 함 |
| 기간을 두 번 연속 확장 | "새로 추가됨" 뱃지가 **최신 확장분에만** 붙고, 첫 번째 확장 때 이미 추가됐던 날짜에는 더 이상 붙지 않는지 (periodExtendedFrom이 최신 값으로 갱신되는지) |

### T24 — 역강등 요청 거절 통지 (신규 — 발견 80)
| 단계 | 기대 |
|---|---|
| 필수 참석자가 역강등 요청 전송 → 주최자가 [거절] | 요청자가 A01 재진입 시 **"요청이 거절됐어요" 배너가 명시적으로 노출**되는지 (이전처럼 대기 배너가 조용히 사라지기만 하는 게 아닌지 확인) |
| 거절 배너의 [확인] | 클릭 시 배너가 사라지고, 이후 재진입해도 다시 뜨지 않는지 (acknowledge 처리 확인) |
| 거절된 이후 같은 멤버가 재요청 | 새 요청이 정상적으로 다시 대기 상태로 뜨는지 (이전 REJECTED 잔여가 새 요청을 가리지 않는지) |

### T25 — 필수 복귀 요청 (신규 — PRD 5.1 [PRD-REINSTATE-REQUEST], 발견 81)
| 단계 | 기대 |
|---|---|
| 강등된 선택 참석자로 그리드 화면 진입 | [다시 필수 참석자가 되고 싶어요] 링크 노출 확인 |
| 필수 참석자로 같은 화면 진입 | 이 링크가 안 보이는지 (이미 필수이므로 대상 아님) |
| 요청 보내기(사유 포함) | 본인 화면에 "대기 중" 배너, 주최자 D01 통합 배너에 **역강등 요청과 별도로** 필수 복귀 요청 배너가 뜨는지(동시에 둘 다 있으면 배너 2개) |
| 주최자 [승인] | attendance가 REQUIRED로 실제 전환되고, 다음 그리드·추천 연산에 필수로 반영되는지 |
| (재현) 주최자 [거절] | 요청자에게 "요청이 거절됐어요" 배너(T24와 동일 패턴) |
| 요청 대기 중 주최자가 회의 확정 | 확정 성공 시 요청 자동 폐기, 재진입해도 잔여 상태 없음 (역강등 요청과 동일 규칙) |

### T26 — 강등 의견 노출 범위 정정 (신규 — 발견 82)
| 단계 | 기대 |
|---|---|
| 부분 성립 카드에서 강등 실행 시 의견 입력 → 확정 | 강등 당사자가 PROGRESS 중 A01 재진입 시 "주최자님의 메모: …" 노출 (기존 유지) |
| 주최자 본인이 D01-DONE(확정 완료 화면) 재진입 | **강등 의견이 이 화면에 노출되지 않는지** — 이전 버전에서 "{이름}님의 의견"으로 잘못 재노출되던 것이 제거됐는지 확인 (이번 회차의 핵심 회귀 포인트) |

### T27 — 재조율 반영 현황 (신규 — PRD 6-2A [PRD-REMATCH], 발견 83)
| 단계 | 기대 |
|---|---|
| 확정 후 참석 취소(경로 A)로 CONFLICT 진입 | R01 화면에 "재조율 반영 현황 0/n명" 카드가 새로 뜨는지, 전원이 "대기" 상태로 시작하는지 |
| 일반 참석자 1명이 그리드 재편집 후 재제출 | 해당 멤버만 "✓ 갱신함"으로 바뀌고, 나머지는 "대기" 유지 |
| 이미 확정 전부터 SUBMITTED였던 멤버가 CONFLICT 중 아직 재제출 안 함 | **"대기"로 표기**되는지 — status가 SUBMITTED라고 해서 "갱신함"으로 잘못 표시되지 않아야 함 (최초 제출 여부와 이탈 후 갱신 여부를 혼동하지 않는지가 핵심 검증 포인트) |
| 대체안 확정 → 새로운 CONFLICT 재진입(EX-05 등으로 재현) | 반영 현황이 0/n으로 리셋되어 있는지 — 이전 재조율 세션의 갱신 기록이 새 세션으로 새지 않는지 |

---

## 4. 합격 기준 (최종 갱신)

기존 전체 기준 + **기간 확장이 전원 통지로 이어지고 참석자 화면에서 신규 날짜만 강조되는지**, **역강등·필수 복귀 요청 거절이 요청자에게 명시적으로 통지되는지**, **필수 복귀 요청이 대칭 경로로 정상 동작하는지**, **강등 의견이 주최자 본인 화면에는 노출되지 않는지**, **CONFLICT 국면의 갱신 현황이 최초 제출이 아닌 이탈 후 갱신 기준으로 정확히 판정되는지**.

### T28 — 재조율 최종 확정 게이팅 (신규 — PRD 6-2B [PRD-REMATCH], 발견 84)
| 단계 | 기대 |
|---|---|
| 확정 후 참석 취소(경로 A)로 CONFLICT 진입 | R01 대체안 카드에 [최종 확정] 버튼이 **없고** "모든 필수 참석자가 갱신하면 최종 확정할 수 있어요" 안내가 대신 뜨는지 (다른 필수 참석자가 아직 미갱신 상태이므로) |
| 이탈 유발 본인(방금 취소한 사람) | 반영 현황 카드에서 본인은 이미 "✓ 갱신함"으로 표시되는지 — 취소 행위 자체가 갱신으로 즉시 인정되는지 확인 |
| 남은 필수 참석자 전원이 그리드 재편집 후 재제출 | 마지막 한 명까지 재제출을 마치는 순간 [최종 확정] 버튼이 카드에 나타나는지 |
| 선택 참석자는 재제출하지 않고 방치 | 필수 참석자만 전원 갱신하면 게이팅이 풀리는지 — 선택 참석자 미갱신은 차단 사유가 아님을 확인 |
| 패널 [EX-05 이탈 발생시키기](경로 B, 웹훅)로 재현 (v2.3부터 기대 결과 변경) | 이탈자(최개발)도 반영 현황 카드·게이팅 대상 목록에 **다른 필수 참석자와 동일하게 등장**하며 처음엔 "… 대기"인지 — 경로 A와 달리 이탈 발생 자체로는 "✓ 갱신함"이 되지 않음을 확인 |
| 최개발이 A01에서 그리드 재제출 완료 | 그제서야 반영 현황에서 "✓ 갱신함"으로 바뀌는지 — 확정 슬롯 자동 마킹과 본인의 실제 재제출은 별개 사건임을 확인 |

### T29 — H01 조율 기간·회의 후보 날짜 가운데 정렬 (신규 — PRD 3.0-A, 발견 85)
| 단계 | 기대 |
|---|---|
| H01 발의 전 화면 진입 | "조율 기간 (종료 시각이 응답 마감이에요)" 라벨과 그 아래 날짜·시각 입력 행이 카드 안에서 가운데 정렬되어 있는지 |
| 같은 화면의 "회의 후보 날짜" 카드 | 라벨과 입력 행(또는 발의 후 표시형 텍스트)이 동일하게 가운데 정렬인지 |
| 발의 후 재진입(잠긴 표시형 상태) | 표시형으로 바뀐 뒤에도 가운데 정렬이 유지되는지 |

### T30 — 기간 확장 시드 누수 정정 + 제출 현황 재대기 (신규 — PRD 5.1 액션③, 발견 86)
| 단계 | 기대 |
|---|---|
| 부분 성립 카드에서 [기간 넓혀서 다시 찾기] 실행 직후 | 대시보드 "제출 현황" 카드가 **"제출 현황 0/6명 (기간 확장 — 새 날짜 응답 필요)"** 류로 즉시 재대기 상태를 보여주는지 — 이전처럼 "전원 제출 완료"로 남아있으면 결함 |
| 히트맵(또는 "다 같이 되는 시간 보기") 펼침 | 새로 추가된 날짜(예: 07/19, 07/20) 칸이 **초록이 아니라 빈 칸("아직 안 정함")** 으로 시작하는지 — 확장 직후부터 이미 다 채워진 것처럼 보이면 결함(이번 회차 핵심 회귀 포인트) |
| A01에서 참석자 1명이 재진입 → 새 날짜만 채우고 제출 | 대시보드 제출 현황이 그 1명만 "✓"로 바뀌는지, 나머지는 "대기" 유지 |
| 전원이 새 날짜를 채워 제출 완료 | 제출 현황이 "N/6명" 정상 카운트로 돌아오는지 |
| 기간을 두 번 연속 확장 | 두 번째 확장 시 제출 현황이 다시 0으로 리셋되는지 — 첫 번째 확장 때 이미 재제출했던 사람도 두 번째 확장의 새 날짜에 대해서는 다시 "대기"로 표시되는지 |

### T31 — shadcn/Tailwind 토큰 이식 시각 검증 (신규 — DS v2.5·v2.6, 12차 수정본)
| 단계 | 기대 |
|---|---|
| 브라우저 개발자 도구로 임의 카드 요소 검사 | Geist 웹폰트가 실제로 로드·적용됐는지 (`document.getElementById("meetsync-geist-font")` 존재 확인 또는 computed font-family) |
| 컨테이너 카드(예: 추천 카드) 모서리 | 곡률이 14px(rounded-xl)로 보이는지 — 이전보다 살짝 각진 인상 |
| 버튼·인풋 모서리 | 곡률이 8px(rounded-md)로 보이는지 |
| 그리드에서 AVAILABLE·AVOID 슬롯 색상 | success(`#02B541`, 선명한 초록) / warning(`#E39219`, 밝은 주황)로 보이는지 — 이전 emerald/amber와 톤이 다름을 인지 |
| "다 같이 되는 시간 보기" 히트맵 | 6개 원색이 아니라 success 단일색의 6단계 오퍼시티(옅음→진함)로 보이는지 |
| 확정 슬롯 | 배경이 실제로 칠해져 있는지 (`bg-primary` 리터럴 미인식 버그, 발견 45가 config 등록으로 재발하지 않는지) + `ring-success` 마커 |
| 탐색 선택 슬롯 | `ring-ring`(중성 회색) 마커가 confirmed 마커(`ring-success`)와 시각적으로 구분되는지 |
| 시나리오 패널·데모 가이드 화면 | devpanel 톤(어두운 배경)이 유지되고, 강조 텍스트가 흰색(`primary-foreground` 재사용)으로 여전히 잘 보이는지 |

### T32 — 발의 시점 긴 후보 기간 + 최대 7일 제약 (신규 — PRD v2.9/3.0-A, 발견 88)
| 단계 | 기대 |
|---|---|
| H01에서 회의 후보 날짜 종료일을 시작일 + 6일(7일째)로 선택 | 선택 가능한지(최대 7일까지는 허용) |
| 같은 입력에서 시작일 + 7일(8일째) 이상 선택 시도 | 날짜 선택기에서 애초에 선택 불가능한지(`max` 제약으로 차단) |
| 후보 기간을 5일로 설정하고 발의 | 배경 응답 유입이 진행되는 동안, 유입되는 멤버의 가용성이 **5일 전체**를 채우는지 — 이전처럼 3일치만 채우고 상태만 SUBMITTED로 바뀌면 결함(발견 88 핵심 회귀 포인트) |
| 위 상태에서 대시보드 "제출 현황" | 실제로 응답이 없는 날짜가 있다면 게이팅이 걸려 "전원 제출 완료"로 오판하지 않는지 |
| 히트맵 4~5일차 | 유입된 멤버들의 실제 가용성(AVAILABLE/AVOID)이 정상적으로 반영되어 있는지 — 빈 칸(UNSET)이 아님을 확인 |

### T33 — CONFLICT 통합 플로우: 취소 강제 진입 + 원클릭 재확인 (신규 — PRD v3.0/6-2C, 발견 89)
| 단계 | 기대 |
|---|---|
| COMPLETED 상태에서 필수 참석자 1명이 참석 취소 확인 | "다른 시간을 찾고 있어요" 대기 화면을 거치지 않고, 곧바로 QUICK 또는 GRID 화면으로 바로 진입하는지 |
| 그 사람의 기존 데이터가 1순위 추천 슬롯을 이미 커버하는 경우 | QUICK 화면("여전히 가능하신가요?")이 뜨는지 — 그리드 전체가 아니라 원클릭 확인만 요구하는지 |
| QUICK 화면에서 "네, 여전히 가능해요" 클릭 | 재조율 반영 현황에 그 사람이 즉시 "✓ 갱신함"으로 표시되는지, 가용성 데이터 자체는 변경되지 않는지 |
| 클릭 직후 같은 화면 | 버튼이 "✓ 반영 완료" 라벨의 비활성 상태로 바뀌는지(재클릭해도 아무 반응 없는지), "다른 시간을 직접 고를게요"·"뒤로가기" 대신 "돌아가기" 단일 버튼만 남는지(발견 90) |
| QUICK 화면에서 "뒤로가기" 클릭 | 참석 취소 자체가 취소되고 COMPLETED 상태·확정 슬롯 가용성이 취소 직전으로 정확히 복원되는지 |
| 그 사람의 기존 데이터가 1순위 추천 슬롯을 커버하지 않는 경우 | QUICK을 건너뛰고 곧바로 GRID(전체 재편집) 화면으로 가는지 |
| GRID 화면에서 "뒤로가기" 클릭(취소 강제 진입 경로로 왔을 때) | 마찬가지로 참석 취소 자체가 취소되는지 — 일반 "본인 재선택(AUTH)" 화면으로 돌아가면 결함 |
| GRID에서 실제로 새 시간 제출 완료 | 이후 그 화면을 다시 들어가도(다른 경로로) 뒤로가기가 더 이상 취소를 되돌리지 않는지(스냅샷이 정리됐는지) |
| 웹훅(경로 B)·뒤늦은 제출(경로 C)로 CONFLICT 진입한 경우 | A01에서 본인 선택 시 동일하게 QUICK/GRID 자동 판정이 적용되는지 — 경로 A와 다르게 동작하면 결함(호스트/참석자·경로 무관 통일 원칙, 5.5) |
| 1순위 슬롯이 부분 성립(레벨 3)뿐인 경우 | 아무도 QUICK을 못 보고 전원 GRID로만 가는지 |

### T34 — 좁은 뷰포트 반응형: 그리드 컬럼폭 + 닫기 버튼 터치 타겟 (신규 — PRD 1.7-B [PRD-RESPONSIVE], 발견 91)
| 단계 | 기대 |
|---|---|
| 모바일 폭(375~390px)에서 후보 기간 7일(최대치)로 A01 그리드 진입 | 요일 컬럼이 시각적으로 찌그러지지 않고, 각 컬럼 폭이 44px 아래로 줄지 않는지 — 컨테이너가 그리드 전체 폭만큼 가로 스크롤되는지 |
| 위 상태에서 그리드를 가로로 스크롤 | 시간 라벨 열(09:00~17:00)이 화면에 고정되어 스크롤 중에도 몇 시 행인지 계속 보이는지 |
| 후보 기간 3일(기본값)로 동일 폭에서 그리드 진입 | 가로 스크롤 없이 컬럼이 화면 안에서 자연스럽게 넓게 표시되는지(스크롤이 불필요하게 걸리면 결함) |
| 알림 배너(AB01) 노출 상태에서 닫기(✕) 탭 | 글자 자체보다 넓은 여백에서도 탭이 인식되는지 — 개발자 도구로 실측 시 히트박스가 44×44 이상인지 |
| 시나리오 패널(P01) 열고 닫기(✕) 탭 | 동일하게 44×44 이상 히트박스로 인식되는지 |
| 데스크톱 폭(1024px 이상) | 그리드 컬럼이 과도하게 넓어지지 않고 `max-w-xl` 카드 폭 안에서 균등 분배되는지 — 좁은 화면 대응이 넓은 화면에서 레이아웃을 깨지 않는지 확인 |

### T35 — 컴포넌트 아키텍처 리팩터 무회귀 확인 (신규 — DEV 3.0 [DEV-COMPONENT-ARCH], 발견 92)
| 단계 | 기대 |
|---|---|
| L01→H01(발의)→D01(대시보드, 응답 유입 대기)→A01(참석자 6인 전원 제출)→D01(추천 확정)→A01(참석 취소)→R01(재조율)→A01(원클릭 재확인) 전 구간 순회 | 리팩터 전과 화면별 문구·레이아웃·동작이 100% 동일한지 — 브라우저 콘솔에 에러가 한 건도 없는지 |
| 시나리오 패널(Ctrl+Shift+D) 열기, EX-04/EX-05/가상 시계 토글 | 패널의 모든 토글·힌트·점프 버튼이 정상 동작하는지 |
| 히트맵(HeatView) 펼치기 → 슬롯 선택 | 인원수별 오퍼시티·확정 마커·상세 목록이 정상 표시되는지 |
| `harness`의 로직 회귀 테스트 14종 | 전부 통과하는지(구조만 바뀌고 로직은 안 바뀌었다는 근거) |

### T36 — 부트스트랩 자체 완결: Tailwind/Geist 폰트 (신규 — [DS-TOKEN 구현 규칙], 발견 93)
| 단계 | 기대 |
|---|---|
| index.html에 Tailwind CDN `<script>` 태그를 아예 빼고, React/ReactDOM만 있는 상태로 앱 로드 | 배경색·간격·radius 등 디자인 시스템이 그대로 적용되는지 (`window.tailwind.config`에 15개 커스텀 색상이 존재하는지 콘솔에서 확인) |
| 브라우저 콘솔에서 `document.fonts` 확인 | Geist 4개 굵기(400/500/600/700)가 실제 @font-face로 등록되어 있는지 — 이전엔 0개였음(jsdelivr 404) |
| index.html에 옛 방식대로 Tailwind CDN `<script>`를 수동으로 남겨둔 경우(하위 호환) | 중복 주입 없이 정상 동작하는지 |
| Network 탭에서 `geist-sans/style.css` 요청 | 더 이상 존재하지 않는지(그 URL 자체를 참조하지 않아야 함) |

### T37 — 실무자 셀프서브: 문서만 보고 코드에서 실제로 찾아지는지 (신규 — 발견 94)
| 단계 | 기대 |
|---|---|
| 00_alignment_guide.md §2를 읽고 app.js에서 그 변수명으로 검색 | `const T = {`가 실제로 검색되는지 (예전엔 문서가 `DESIGN_TOKENS`라고 안내해 검색 결과 0건이었음) |
| 00_alignment_guide.md §3를 읽고 app.js에서 그 이름으로 검색 | `function buildSeedData()`가 실제로 검색되는지 (예전엔 `SEED_MEETING_DATA` 안내로 검색 결과 0건이었음) |
| 히트맵(D01/R01)에서 아무도 응답 안 한 슬롯(0인원) 확인 | 배경색이 `secondary` 토큰 값과 정확히 같은지 — 코드에 `bg-[#EFEEF0]`처럼 토큰 밖 하드코딩이 남아있지 않은지 |
| app.js 전체에서 `#[0-9A-Fa-f]{3,6}` 패턴(임의 hex) grep | tailwind.config 선언부 외에는 0건인지 |

---

## 5. 합격 기준 (최종 갱신)

기존 전체 기준 + **재조율 중 필수 참석자 전원이 갱신하기 전에는 최종 확정이 실제로 불가능한지(버튼 자체가 안 보이는지)**, **이탈 유발 본인은 별도 재제출 없이 즉시 반영 완료로 인정되는지**, **H01 두 기간 카드가 가운데 정렬인지**, **기간 확장 직후 신규 날짜가 어떤 참석자에게도 미리 채워져 있지 않고 제출 현황이 즉시 재대기로 전환되는지**, **shadcn 토큰(색·곡률·서체)이 화면 전역에 실제로 적용되고 로직·구조에는 회귀가 없는지**, **회의 후보 기간이 최대 7일로 제한되고, 발의 시점에 3일보다 긴 기간을 고른 경우에도 배경 유입 데이터가 그 기간 전체를 정확히 채우는지**, **참석 취소가 대안 선택 없이 끝나는 상태를 만들지 않는지, 기존 데이터로 이미 커버되는 사람은 원클릭으로만 갱신되고 그렇지 않은 사람만 그리드를 다시 채우는지, 뒤로가기가 취소를 정확히 원복하는지**.
