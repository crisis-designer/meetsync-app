# 📄 [문서 1] 마스터 PRD (기획 상세서)

본 문서는 6인 이상 도메인 기반 일정 조율 웹앱의 비즈니스 로직과 시스템 정책을 정의한 기획 상세서입니다. 본 문서에는 디자인 토큰명이나 프론트엔드 코드 문법을 포함하지 않으며, 오직 서비스의 기능적 명세와 예외 처리 규칙만 기술합니다.


---

## 문제 발견과 해결 — 과제 제출 답변 `[PRD-ANSWER]`

본 섹션은 이 문서 전체의 총론이자 과제 3개 질문에 대한 공식 답변이며, **제출문과 README의 단일 원본**이다 (제출 채널에 옮길 때 문구 임의 변형 금지). 아래 모든 명세는 이 답변의 증명으로 존재한다. **동기화 규칙:** QA·설계 검토로 문제 정의(2.1)가 개정되면 본 섹션도 반드시 같은 이터레이션에서 함께 개정한다 — 총론과 본문이 다른 말을 하는 문서는 결함이다.

### Q1. 사용자의 어떤 문제를 발견했나요? 그것이 왜 문제라고 생각했나요?

6명이 회의를 못 잡는 이유를 관찰하면 계산이 어려워서가 아니다 — 27개 슬롯과 6명의 조건을 교차하는 건 산수다. 진짜 문제는 조율 과정에서 사람들이 지불하는 **관계 비용**이다.

주최자는 미응답자를 재촉하는 게 미안하고, 누군가의 비선호 시간으로 확정하는 순간 "왜 하필 그 시간이냐"는 원망의 수신인이 되며, 확정 후 한 명이 빠지면 이 모든 대화를 처음부터 반복해야 한다. 참석자는 "가능"이라 답하면 그 시간에 박제될까 봐 보수적으로 답하고, "되긴 되는데 점심 직후는 좀…" 같은 진심은 단톡방에서 말하면 까다로운 사람이 되니까 삼킨다. 그래서 결국 목소리 큰 사람의 조건이 이기고, "저는 아무 때나 괜찮아요"라고 말한 사람이 손해 본다.

기존 도구들은 계산(투표, 가능/불가 취합)만 도울 뿐, 사람들이 실제로 괴로워하는 이 비용은 전혀 다루지 않는다 — 이것이 도구가 있는데도 여전히 단톡방에서 일정을 잡는 이유다.

### Q2. 그 문제를 어떻게 해결했나요?

관계 비용을 항목별로 시스템에 이관했다.

- **눈치의 비용** → "가능하지만 피하고 싶다"(비선호)를 시스템에만 말하게 해서 진심의 사회적 비용을 0으로 만들었고, 물리적으로 무리한 시간은 이동 버퍼가 본인 대신 거절한다.
- **재촉의 비용** → 마감 도달 시 시스템이 미응답자를 특정하고 독촉을 대행한다.
- **결정 책임의 비용** → 추천마다 산출 근거(전원 가능/비선호 포함/부분 성립)와 양보하는 사람의 실명을 표기해, 주최자에게 "조건상 이것이 최선"이라는 방어 가능한 근거와 사전에 양해를 구할 대상을 쥐여준다. 집계 히트맵으로 주최자가 동료들의 상황 자체를 이해할 수 있게 했다.
- **번복의 비용** → 확정 직전의 변동(동시성 검증)과 확정 후 이탈(재조율)을 정식 플로우로 설계해, 이탈이 생겨도 이미 모은 5명의 응답은 죽지 않는다.
- 그리고 이 모든 것에 참여시키는 **마찰**을 없애기 위해, 참석자는 가입 없이 링크만으로 응답한다.

### Q3. 왜 이 방식으로 설계했나요?

조율 도구의 실패는 해피 패스가 아니라 엣지에서 온다 — 전원 가능한 시간이 없을 때, 한 명이 응답하지 않을 때, 확정 후 일정이 깨질 때. 그 순간마다 도구가 침묵하면 사람들은 단톡방으로 돌아가고, 관계 비용은 고스란히 부활한다.

그래서 예외 다섯 가지를 부록이 아닌 1급 설계 대상으로 삼았고, 특히 "결과 없음"이라는 상태는 3단계 완화(참조자 제외 → 비선호 격상 → 부분 성립)로 구조적으로 제거했다 — 시스템은 어떤 데이터 조합에서도 반드시 답과 그 근거를 내놓는다. 근거를 항상 실명과 함께 노출하는 건, 자동화의 목적이 결정의 대행이 아니라 **결정의 책임을 사람에게서 덜어내는 것**이기 때문이다.

---

## 0. 버전 히스토리

| 버전 | 변경 내용 |
|---|---|
| v1.0 | 최초 작성 (핵심 플로우 + 예외 처리 5종 + 데이터 정책) |
| v1.1 | 제품 정체성(데모) 확정에 따른 전면 보강 — 시뮬레이션 경계 선언(섹션 1), 그리드 편집 해피 패스(섹션 3), 회의 상태 머신(섹션 4), 추천 연산 정렬 정책(3.4) 신설, EX-03 가상 시계 재정의, EX-04 전체 리로드 금지, EX-05 도달 경로 명시 |
| v1.2 | 1차 QA 발견 8건 반영 — 재편집(3.5)·뒤로가기(3.6)·알림 등급(1.5)·컨텍스트 가시성(2.3)·R01 가드(6.5)·강제 마감 라벨 보정(3.4) 신설, 불참 선언 OUT 기록(8) |
| v1.3 | 2차 QA 발견 7건 + 과제 컨텍스트 반영 — 과제 브리프 매핑(2.4), 여정 완결성(2.5), 화면 진입 가드(2.6), 평가자 데모 가이드(1.6) 신설, 발의 화면 설정 표시(2.2), EX-04 실데이터 변동(5.4), 참조자 미응답 판정(3.7), 배너 해소 규칙(1.5) |
| v1.4 | 여정 전면 재검토 발견 8건 반영 — 주최자 발의 직후 동선을 D01+초대 링크 카드로 재설계(2.5-②), 참석자 결과 안내 상태 신설(2.5-⑤) 및 진입 가드 개정(2.6), 완화 3단계 '부분 성립' 신설(3.4, 5.1), 상태 전이 영속화 의무(7.2), EX-05 1회성 소비 규칙(5.5), 주최자 의사결정자 고정(2.2), 강제 마감 후 지각 제출 규칙(5.3), 멀티탭 동기화 [PRD-SYNC](7.6) 신설, 모킹 표에 멤버 디렉토리 로드 추가, OUT 4건 추가(8) |
| v1.5 | 문제 정의를 사용자 관점으로 재정립 — 관계 비용 테제 신설(2.1 [PRD-PROBLEM]), 메커니즘별 사용자 근거 매핑(2.1), 주최자 집계 가시성 뷰 신설(3.8 [PRD-HEATMAP]), 추천 카드 인원 실명 표기(3.4), '나머지 모두 가능' 일괄 입력(3.3), 브리프 매핑에 '동료 상황 이해' 행 추가(2.4) |
| v1.6 | 과제 제출 답변 3종을 부록으로 편입(9 [PRD-ANSWER]) — 제출문·README의 단일 원본으로 지정 |
| v1.7 | [PRD-ANSWER]를 문서 최상단 무번호 총론으로 이동(구 섹션 9 삭제) — 모든 하위 명세는 총론의 증명이라는 위계 확립, 2.1 개정 시 동시 개정 규칙 유지 |
| v3.0 | 14차 QA 발견 1건(89) 반영 — **CONFLICT 통합 플로우 신설**: (1) 참석 취소(경로 A) 확인 즉시 대기 화면 없이 곧바로 대안 선택으로 강제 진입, 뒤로가기는 취소 자체를 취소; (2) 1순위 추천 슬롯이 이미 자신의 기존 가용성으로 커버되는 필수 참석자는 그리드 전체 재제출 대신 원클릭 재확인만으로 갱신 인정(6-2C 신설) — 단 v2.7(발견 84)의 게이팅 자체(전원이 명시적으로 갱신해야 확정 가능)는 유지, "옛 데이터로 조용히 확정"이 아니라 "그 슬롯을 직접 보여주고 지금 이 순간 명시적으로 확인받는" 것이므로 게이팅의 취지를 훼손하지 않는다. 호스트/일반 참석자, 이탈 경로(A/B/C) 무관하게 동일 로직 적용 |
| v2.9 | 13차 QA 발견 1건(88) 반영 — **회의 후보 기간 최대 7일 정책 신설**(실무 데이터 근거, 3.0-A), 배경 응답 유입 시뮬레이션이 고정 3일이 아닌 그 회의의 실제 candidatePeriod 전체를 기준으로 데이터를 생성하도록 원칙 명시 — 발의 시점에 3일보다 긴 후보 기간을 선택하면 일부 날짜가 무응답인데도 전원 제출 완료로 표시되던 결함 시정 |
| v2.8 | 12차 QA 발견 1건(87) 반영 — **경로 B(웹훅 이탈) 특별취급 전면 폐지**: 필수 참석자가 외부 캘린더 충돌로 자동 이탈 처리될 때 본인만 재조율 연산·갱신 대상에서 제외되고 본인 재편집 동선도 막혀 있던 구조를 제거, 경로 A·C와 동일하게 연산 참여·갱신 요구·재편집 동선을 부여([PRD-EX-05], [PRD-REMATCH] 6·6-2B, 2.5-⑤, 2.6 개정). 단, 경로 A(본인 선언)와 달리 경로 B는 본인이 취한 행동이 없으므로 "이탈 선언=즉시 갱신 인정" 특례는 적용하지 않고, 대신 감지 시점에 확정됐던 슬롯을 본인 가용성에서 자동 마킹해 동일 슬롯 재추천을 차단(87) |
| v2.7 | 11차 QA 발견 3건(84~86) 반영 — CONFLICT 재조율 반영 현황이 표시에 그치던 것을 **최종 확정 차단(게이팅)**으로 강화([PRD-REMATCH] 보강, 84), H01 조율 기간·회의 후보 날짜 카드를 정렬 규칙(1.7-A)의 명시적 예외로 지정해 가운데 정렬 확정(85), 기간 확장 시 신규 날짜가 미리 채워진 가짜 데이터로 보이던 구조적 결함 정정 — 신규 날짜는 항상 빈 상태로 시작하고 제출 현황도 재대기로 돌아가야 함([PRD-EX-01] 보강, 86) |
| v2.6 | 10차 QA 발견 5건(79~83) 반영 — 기간 확장 시 전원 통지 신설([PRD-EX-01] 보강, 79), 역강등 요청 거절 시 요청자 통지 추가([PRD-PROMOTE-REQUEST] 보강, 80), **필수 복귀 요청**([PRD-REINSTATE-REQUEST] 신설 — 역강등 요청의 대칭 경로, 선택 참석자가 필수 복귀를 요청, 81), 강등 의견 노출 범위 오류 정정 — 확정 완료 화면(D01-DONE) 노출은 애초에 요구사항이 아니었음, PROGRESS 배너 단독 노출로 정정(82), CONFLICT 국면 "재조율 반영 현황" 카드 신설(83, [PRD-REMATCH] 보강) |
| v2.5 | 9차 QA 발견 4건(74~77) 반영 — 주최자 통합 배너를 역강등 승인/거절 전용으로 좁히고 재요청 액션은 카드에서만 노출(74), EX-04 확정 실패 시 상태 갱신을 항상 새 참조로 생성해 보장(75), 강등·역강등 사유 텍스트를 상대방이 실제로 보는 화면에 노출하는 것을 명시적 요구사항으로 확정(76), **조율 기간(coordinationPeriod, 이 제품으로 회의 후보 날짜를 정해가는 실시간 진행 구간)과 회의 후보 날짜(candidatePeriod, 참석자가 그리드에서 실제로 고르는 대상)를 서로 다른 개념으로 완전 분리**(77, [PRD-PERIOD-SPLIT] 신설 — 근거는 레퍼런스 제품의 유무가 아니라 우리 제품 자체의 논리: 조율이 진행 중인 날짜를 회의 후보로 동시에 제시하는 것은 아직 결론 나지 않은 것을 이미 확정된 선택지처럼 보여주는 모순이다) |
| v2.4 | 8차 QA 발견 8건(66~73) 반영 — 화면 컴포넌트 리마운트로 인한 텍스트 입력 불가 버그 수정(66, 구조적 원인), 회의 시간 단위·조율 기간·응답 마감 주최자 직접 입력 개방(67·69), 드롭다운 커스텀 화살표(68), 역강등 요청 사유 입력 추가(71), 통합 배너-카드 재요청 중복 제거(72), 재확정 후 잔존 배제 사유로 인한 불참 오표기 수정(73, [PRD-CANCEL-SCOPE] 적용 재확인) |
| v2.3 | 본질 문제 재검토 반영 — CANCELLED 상태 신설([PRD-CANCEL-MEETING]), 역강등 요청(승인 기반, [PRD-PROMOTE-REQUEST]), 강등 시 의견 남기기, 기간 확장 재탐색([PRD-EX-01] 확장), 제출 시점 프로액티브 재요청([PRD-PROACTIVE]), 일 단위 일괄 불가 입력, 주최자 통합 행동 배너([PRD-NEXT-ACTION]), 표시 문구 순화(분류명 유지), 발의 후 제목 잠금, 재진입 경로 영구 매트릭스(2.5-⑤ 확장) |
| v2.2 | 7차 QA 발견 10건(56~65) 반영 — 그리드 위계 실제 코드 반영(56), EX-04 확정 실패 슬롯 명시적 배제(61), 히트맵 균등 명도 스케일 재조정(58·62), 미확인자 재진입 시 "뒤늦은 제출" 경로 신설([PRD-LATE-JOIN] — 60), 배제 사유 4종으로 확장(SELF_CANCEL·WEBHOOK·LATE_MISMATCH·강등, 3.4·5.5·2.5-⑤), 강등 배너에 근거 슬롯 명시(64) |
| v2.1 | 6차 QA 발견 10건(46~55) 반영 — 정렬 우선순위 역전(비선호 인원 최소화 최우선, 3.4), 미확인/참석불가/자발적불참 3분기 사유 표기 및 확정 문장 보정([PRD-ABSENCE-REASON] 신설, 3.4·2.5-⑤), 독촉 알림 당사자 가시성 추가(2.7·5.3), 히트맵 비균등 명도 스케일(3.8), 반응형·터치 타겟 원칙 신설(1.9 [PRD-RESPONSIVE]), UI 정보 위계·정렬 일관성 규칙(1.8 확장), Top3 근거 펼침 옵션(3.4) |
| v2.0 | 5차 QA 발견 6건(38~43) 반영 — 참석 취소(경로 A) 재설계: 슬롯 단위 차단 + 재조율 참여 유지(5.5 개정), 이탈자 표기-재연산 정합([PRD-CANCEL-SCOPE] 신설), 히트맵 명도 스텝 확대·확정 슬롯 계열 분리(3.8), 지속 배너 주최자 화면 한정(1.5), 제품 내 사용자 노출 문구 전면 재검수(1.8 [PRD-COPY] 신설 — 조직/개발 용어 제거) |
| v1.9 | 4차 QA 발견 9건(28~37) + 정책 확정 반영 — COMPLETED 불변식 및 필수 인원 배제의 통합 3요소 원칙 신설(2.7 [PRD-EXIT-PRINCIPLE]), 강행 확정('불참 확정') 폐기 및 강등 경로 신설(5.1), 참석 취소 선언의 제품 내 실경로(5.5 이원화), 강제 마감 확인·독촉 기록(5.3), 확정 확인을 확인 영역 패턴으로 재설계(3.4), 발의 후 권한 토글 잠금(2.2), 미발의 가드(2.6), 당사자별 안내 상태(2.5-⑤), 잠금 출처 표기(3.2), 히트맵 인원수 램프·마커 분리(3.8), 재요청 발송 상태(5.1), 유입 트리거를 발의 사건으로(1.1), 커밋 지점 ⑦⑧⑨ 확장(7.2) |
| v1.8 | 3차 QA 발견 9건(19~27) 반영 — 유저 지정 '불가' 상태 신설(3.2/3.3), 불가 포함 확정 정책·확정 2탭 확인·등록 문구 보정(3.4), 교착 해소 플로우 [가용성 재요청]/[불참 확정](5.1), 응답 유입 연출로 모킹 전환(1.1), 데모 관찰 가능성 규칙 신설(1.7 [PRD-SIM-OBSERVE]) 및 패널 힌트·EX-05 단발 버튼화(1.4, 5.5), EX-04 배너 제외 슬롯 명시(5.4), 추천 카드 서브라인 중복 제거(3.4), COMPLETED 대시보드 결과 중심 위계(2.5-⑥), 가이드 스캔 구조(1.6), 2.1 매핑표 3행 추가 |

---

## 1. 시뮬레이션 경계 선언 `[PRD-SIM]`

### 1.1 제품 정체성

본 제품은 **포트폴리오 데모**다. 서버 백엔드 없이 단일 클라이언트 환경에서 구동되며, 시연을 보는 사람이 실제 서비스와 구분할 수 없는 수준의 동작 재현을 목표로 한다. 이를 위해 모든 기능은 아래 두 부류로 명확히 구분하며, 이 경계는 본 문서의 모든 하위 명세에 우선 적용된다.

**진짜로 구현하는 것 (로컬에서 실제 동작 가능한 전부)**
- 타임슬롯 그리드 편집 (탭/드래그 입력, 상태 전환) — 섹션 3
- 가용성 데이터 및 회의 상태 전이의 localStorage 영속화 — 섹션 7
- 추천 연산 알고리즘 (교집합, 완화 1·2·3단계, Top 3 정렬) — 섹션 3.4, 섹션 5.1
- 회의 상태 머신 전이 (PROGRESS → COMPLETED → CONFLICT) — 섹션 4
- 마감 판정 로직 — 단, 기준 시각은 1.3의 가상 시계
- EX-04 충돌 시의 조율 데이터 실변동 및 추천 재연산 — 섹션 5.4
- 브라우저 탭 간 회의 데이터 실시간 동기화 (storage 이벤트) — 섹션 7.6
- 참석 취소 선언과 CONFLICT 전이, 권한 강등, 발송 기록 — 섹션 5.5, 5.1, 5.3

**모킹으로 대체하는 것 (서버 없이는 원천 불가능한 것만)**

| 기능 | 모킹 방식 | 규칙 |
|---|---|---|
| 도메인 멤버 디렉토리 연동 | 발의 화면의 멤버 6인 목록을 "회사 디렉토리에서 자동 로드된 결과"로 간주 | 시드 고정. 멤버 추가·삭제 기능 없음 (8 OUT) |
| 외부 캘린더 API 연동 | 시드 데이터를 "외부 캘린더에서 파싱된 결과"로 간주 | EX-02 이동 버퍼는 시드 생성 시점에 이미 반영된 상태로 제공 |
| 참석자 본인 인증 | 참석자 화면 진입 시 멤버 목록에서 본인을 선택하는 단계로 대체 | 선택한 멤버의 시점으로 그리드가 렌더링됨 |
| 초대 링크 발급·공유 | 링크 카드에 가상 URL 표기 + [링크 복사] 연출. [링크 열어보기]로 초대 랜딩 진입(2.5) | 실제 URL 발급·클립보드 공유 대상 없음 |
| 타 참석자 5인의 제출 행위 | **응답 유입 연출 (v1.8):** 시드는 전원 미제출로 시작하고, 발의 후 대시보드 도착 시점부터 5인(m1~m5)의 응답이 약 1초 간격으로 순차 유입된다. 유입 각 건은 실제 localStorage 커밋이다 — 유입 도중 새로고침해도 시스템은 정합 (미제출자가 더 있는 상태일 뿐) | 발의 전에 5인이 이미 제출 완료인 인과 붕괴를 금지한다. 미제출자(m6) 1인은 EX-03 시연용으로 끝까지 PENDING 유지 |
| 독촉 알림 발송 | 발송 완료 연출만 수행. 단, **발송 기록은 실데이터로 커밋**한다 (5.3 확인 영역이 참조) | 실제 발송 없음. 수신 재현 불가 — 당사자는 링크 재진입 시 화면 상태로 인지 (2.7) |
| 가용성 재요청 발송 (5.1) | 발송 완료 연출만 수행. **발송 기록은 실데이터로 커밋** (강등 확인 영역이 참조) | 실제 발송 없음. 병목 분석(누구에게 어떤 슬롯을)은 실연산 |
| 확정 시 전원 캘린더 등록 | 등록 완료 연출만 수행 | 실제 API 호출 없음 |
| 웹훅/폴링 일정 변동 감지 (EX-05 경로 B) | 시나리오 패널의 단발 버튼으로 발동 | 1.4 참조. **참석 취소의 제품 내 선언(경로 A — 5.5)은 모킹이 아니라 실구현**이다 |
| 확정 순간 동시성 충돌 (EX-04) | 시나리오 패널 토글로 발동. 단, 충돌 발동 시의 데이터 변동은 실구현(5.4) | 토글 OFF 시 확정은 정상 성공한다. **무조건 발동 금지** |

> **원칙: 로컬에서 진짜 가능한 것을 연출로 때우는 것을 금지한다.** (예: localStorage 커밋을 알림 문구로 대체하는 행위, "갱신했습니다" 문구만 있고 실제 갱신이 없는 행위) 반대로, 모킹 대상을 실제 구현인 것처럼 문서에 기술하는 것도 금지한다.

### 1.2 데모 기준 페르소나 플로우

시연자(또는 심사자)는 하나의 브라우저에서 주최자와 참석자 역할을 오간다. 두 개의 창/탭을 나란히 두고 역할별로 나누어 보는 것도 지원한다(7.6).

1. **랜딩(L01)**에서 서비스가 해결하는 문제와 방식을 확인하고 [회의 만들기]로 여정 시작
2. 주최자(김주최)로 회의를 발의 → **대시보드(D01) 진입**, 초대 링크 카드에서 링크 생성 확인
3. 링크 카드의 [링크 열어보기]로 **초대 랜딩** 진입 — "링크를 받은 참석자"의 시점 체험 → 본인 선택(모킹 인증) → 그리드 편집·제출 실제 체험
   - **박개발(m6)을 선택해 제출하면** → 의사결정자 전원 응답 → 정상 결과 산출 (해피 패스)
   - **박개발을 제출하지 않은 채** 가상 시계를 '마감 도달'로 전환하면 → EX-03 발동 (예외 패스)
4. **제출 완료 단계**에서 응답 현황 확인 → [주최자 대시보드에서 결과 보기]로 역할 전환
5. 주최자 대시보드에서 추천 Top 3 확인 및 최종 확정
6. 시나리오 토글에 따라 EX-04(확정 순간 충돌) 또는 EX-05(확정 후 이탈) 시연
7. 확정 후 참석자 링크를 다시 열면(A01 재진입) **확정 결과 안내** 확인 — 참석자 여정의 결말(2.5-⑤)
8. 완료 화면에서 [새 회의 만들기]로 랜딩 복귀 — 여정이 순환 구조로 닫힌다

### 1.3 데모 가상 시계 `[PRD-SIM-CLOCK]`

마감 판정(EX-03)을 실제 시스템 시각으로 하면 데모를 실행하는 날짜에 따라 결과가 달라진다. 이를 차단하기 위해 다음 규칙을 적용한다.

- 시스템의 모든 마감 판정은 실제 시각(Date.now)이 아니라 **가상 시계 상태값**을 기준으로 한다.
- 가상 시계는 두 가지 상태만 갖는다: `마감 전`(기본값) / `마감 도달`
- 상태 전환은 시나리오 패널(1.4)에서만 가능하다.
- 가상 시계는 **탭 로컬**이다 — 탭 간 동기화(7.6) 대상이 아니다. 시나리오 제어는 각 탭의 데모 조작 장치이기 때문이다.
- 화면에 노출되는 마감 시한 문구는 **조율 기간의 종료 시각(coordinationPeriod.end + 시각)**을 그대로 표기한다 (v2.5, 발견 77 — 별도 deadline 필드는 폐기, 조율 기간 종료가 마감 역할을 흡수한다. 3.0-A 참조).

### 1.4 시나리오 제어 패널 `[PRD-SIM-PANEL]`

데모 시연 중 예외 시나리오를 제어하는 숨김 패널. **기본 상태는 완전 숨김이며, 숨김 상태의 화면은 실제 제품과 구분되지 않아야 한다.**

**호출 방식**
- 데스크톱: `Ctrl + Shift + D` 토글
- 모바일: 화면 좌측 상단 로고 영역 2초 내 5회 연속 탭
- 제품 화면(H01·A01·D01·R01)에는 호출 방식 외의 어떤 UI 요소(버튼, 링크 등)도 패널 진입 경로로 노출하지 않는다. 단, 데모 가이드 화면(1.6)에서 호출 방식을 **문서로 안내**하는 것은 허용한다 — 가이드는 제품 화면이 아니다.

**패널 제어 항목**

| 항목 | 옵션 | 기본값 | 연동 예외 |
|---|---|---|---|
| 가상 시계 | 마감 전 / 마감 도달 | 마감 전 | EX-03 |
| 확정 순간 동시성 충돌 | OFF / ON | OFF | EX-04 |
| 확정 후 필수 참석자 이탈 | [이탈 발생시키기] 단발 버튼 (v1.8) | COMPLETED에서만 활성 — 비활성 시 사유 표기 | EX-05 (실행 즉시 소비 — 5.5) |
| 데이터 초기화 | 실행 버튼 | — | localStorage 삭제 + 시드 상태 복원 |

- 토글 상태는 영속화하지 않으며 탭 로컬이다. 새로고침 시 전부 기본값으로 복귀한다.
- **관찰 가능성 (v1.8, 1.7):** 각 제어 항목에는 현재 설정이 시스템에 미치는 효과를 말해주는 힌트 문구를 상시 표기한다. (예: EX-04 ON — "다음 확정 시도는 충돌합니다" / 이탈 버튼 비활성 — "확정 완료 상태에서만 발동합니다") 조작해도 아무 변화가 관찰되지 않는 제어를 금지한다.
- 패널이 열려 있는 동안에도 제품 화면은 정상 조작 가능해야 한다 (패널이 화면 전체를 가리지 않음).
- **닫기 수단:** 호출 제스처 재실행 외에, 패널 우측 상단에 닫기(X) 버튼을 상시 제공한다. 제스처만으로 닫게 하는 것을 금지한다.

### 1.5 알림 등급 정책 `[PRD-NOTICE]`

시스템 피드백은 두 등급으로 구분하며, 등급을 혼용하지 않는다.

| 등급 | 수단 | 소멸 | 대상 |
|---|---|---|---|
| 경량 피드백 | 토스트 (2.5초 자동 소멸) | 자동 | 모킹 연출(링크 복사·독촉 발송·캘린더 등록), 제출 완료, 초기화 완료 |
| 중요 알림 | 지속 배너 (수동 닫기 전까지 유지) | 사용자가 닫기 또는 상황 해소 | EX-04 동시성 충돌, EX-05 이탈 감지 등 사용자의 판단·행동이 필요한 사건 |

> 사용자의 후속 행동이 필요한 정보를 자동 소멸 수단에 담는 것을 금지한다.

- **해소 규칙:** 지속 배너를 유발한 상황이 시스템 상태 변화로 해소되면 배너는 자동으로 내린다. (예: EX-04 충돌 배너는 이후 확정이 정상 성공하는 순간 자동 해제, EX-05 이탈 배너는 재조율 확정 성공 시 자동 해제. "일정 변동" 배너와 "확정 완료" 표시가 동시에 노출되는 모순을 금지한다.)
- **노출 범위 (v2.0 — 발견 42):** 지속 배너는 **주최자 화면(D01·R01) 한정**이다. 참석자 화면(A01)은 당사자별 전용 안내(2.5-⑤)로 이미 같은 정보를 맥락에 맞게 전달하므로 상단 배너를 중복 노출하지 않는다.
- **독촉 알림 당사자 가시성 (v2.1 — 발견 51):** 독촉(5.3)은 발신자(주최자) 쪽에만 흔적이 남고 수신자 쪽은 아무 표시가 없던 것이 2.7 통합 원칙의 가시성 요건 누락이었다. 독촉 발송 기록(`nudgedIds`)에 본인이 포함되어 있으면, 해당 미제출 참석자가 그리드 화면 재진입 시 "주최자님이 빨리 답해달라고 알려왔어요" 배너를 본다.

### 1.6 평가자 데모 가이드 `[PRD-SIM-GUIDE]`

본 데모는 과제 심사자가 사전 지식 없이 URL만 열어보는 상황을 1차 시나리오로 간주한다. 숨김 패널(1.4)은 심사자가 자력으로 발견할 수 없으므로, 다음 규칙으로 시연 동선을 보장한다.

- 별도의 **데모 가이드 화면**을 둔다. 진입 경로는 랜딩(L01) 하단의 소형 텍스트 링크 1곳만 허용한다. 제품 화면에는 노출하지 않는다.
- 가이드 화면은 제품 UI와 시각적으로 구분되는 룩(시나리오 패널과 동일 계열)을 사용한다 — "이것은 데모 사용 설명서"임이 즉시 인지되어야 한다.
- 가이드 내용: ① 권장 시연 순서(1.2 요약) ② 시나리오 패널 호출 방법 ③ 각 예외 시나리오(EX-01~05)의 발동 방법과 관찰 포인트 ④ 두 창 나란히 보기 방법과 경계(7.6 — 데이터는 동기화, 시나리오 토글은 창별) ⑤ 데이터 초기화 방법.
- 가이드는 정적 안내 화면이며, 가이드 화면 자체에 시나리오 토글을 두지 않는다 (제어는 패널로 일원화).
- **스캔 가능 구조 (v1.8):** 문단 나열을 금지한다. 시연 순서는 번호 매긴 단계 카드로, 단축키는 키 캡(kbd) 표기로, 각 예외 시나리오는 **[발동 조건 → 조작 → 기대되는 화면 변화]** 3단 블록으로 조립한다.

### 1.7-A 사용자 노출 문구 규칙 `[PRD-COPY]` (v2.0 신설 · v2.1 확장)

**기준:** 이 화면을 처음 보는 동료가 3초 안에 뭘 해야 할지 알 수 있는가. 조직·개발 용어가 사용자 화면에 새어 나오는 것을 금지한다.

| 금지 (기획/개발 용어) | 사용 (일상어) |
|---|---|
| 가용성, 가용 시간 | 내 시간, 되는 시간 |
| 의사결정자 | 필수 참석자 |
| 참조자 | 선택 참석자 |
| 부분 성립, 완화 단계 | (라벨에서 제거 — 사유 중심 문구로 대체) |
| 응답 현황 | 제출 현황 |
| 조율 근거 | 다른 시간대 비교 |

- 본 표는 **사용자에게 실제 노출되는 문자열**에만 적용한다. 문서 내부 지칭(가용성 스키마, EX-01 등 태그)과 개발자 대상 데모 가이드(GD01)의 기술 설명은 대상이 아니다.
- 신규 문구 추가 시 이 표를 기준으로 자가 검수한다.

**표시 문구 순화 규칙 (v2.3 신설):** 본인이 아닌 **다른 사람이 보는 화면**(히트맵 상세, 추천 카드 서브라인, 완료 화면의 불참 목록)에서 상태를 언급할 때는 말투를 완곡하게 조정한다 — 예: "안 되는 시간" → "다른 일정이 있어요". **단, 내부 분류명(UNCONFIRMED·UNAVAILABLE_SLOT·SELF_DECLINED 등)과 사유 판정 로직은 순화 대상이 아니다** — 정확성을 낮추는 게 아니라 말투만 부드럽게 한다. 3.4 사유 3분기 표기(`[PRD-ABSENCE-REASON]`)의 정보량은 그대로 유지한 채 표현만 조정한다.

**정보 위계 규칙 (v2.1 — 발견 46):** 사실 정보(매번 확인해야 하는 값 — 마감일, 현황 수치)와 사용법 안내(한 번 학습하면 되는 조작 설명)를 같은 시각적 무게로 묶지 않는다. 사용법 안내는 보조 정보로 낮은 위계(작은 글씨·낮은 대비)에 배치한다.

**정렬 일관성 규칙 (v2.1 — 발견 47):** 카드 본문(라벨·서브라인·이름 목록 등 정보 전달용 텍스트)은 기본적으로 좌측 정렬이다. 확인 영역(`[DS-COMP-CF01]`)처럼 사용자의 결정을 요구하는 무게 있는 상호작용 블록만 예외적으로 중앙 정렬을 쓴다. 부모 컨테이너의 정렬 속성을 자식 정보 텍스트가 그대로 상속받아 의도치 않게 중앙 정렬되는 것을 금지한다 — 정렬은 컴포넌트 단위로 명시한다. **추가 예외 (v2.7, 발견 85):** H01의 조율 기간·회의 후보 날짜 카드는 3.0-A에 명시된 대로 가운데 정렬한다.

### 1.7-B 반응형·터치 상호작용 원칙 `[PRD-RESPONSIVE]` (v2.1)

본 제품은 웹과 모바일 앱 양쪽에서 쓰인다는 전제로 설계됐으나(총론 참조), 실제 검증은 데스크톱 브라우저 프리뷰에서만 이뤄져 왔다 — 이는 검증 공백이며, 아래 원칙을 신규 기준으로 삼는다 (발견 52~55).

- **터치 타겟 최소 크기:** 탭으로 조작하는 모든 대상(그리드 슬롯, 히트맵 셀, 버튼)은 최소 44×44px를 확보한다. 특히 드래그로 연속 입력되는 그리드는 오탭의 대가가 크므로(엉뚱한 슬롯이 잘못 채워짐) 반드시 준수한다.
- **패널·오버레이의 화면 점유 제한:** 시나리오 패널처럼 화면 하단에 고정되는 UI는 좁은 뷰포트에서 여러 줄로 쌓이며 본문을 가리는 것을 방지하기 위해, 내부 스크롤 또는 접힌 요약 상태를 갖는다.
- **텍스트 오버플로 방지:** 이름·역할이 붙는 뱃지 등 가변 길이 텍스트가 포함된 컴포넌트는 좁은 화면에서 잘리거나 줄바꿈으로 레이아웃이 깨지지 않도록 말줄임(`truncate`) 또는 우선순위 기반 생략을 적용한다.
- **검증 의무:** 핵심 화면(그리드 편집, 대시보드, 확인 영역)은 최소 1회 이상 좁은 뷰포트(360~390px 기준) 렌더링으로 검증한다.

### 1.7 데모 관찰 가능성 규칙 `[PRD-SIM-OBSERVE]` (v1.8)

데모 장치(패널·시나리오·실데이터 변동)는 조작의 인과가 시연자에게 보이지 않으면 함정이 된다. 다음을 강제한다.

- 모든 데모 제어는 조작 시 그 효과(또는 효과가 유예되는 조건)를 즉시 화면에서 확인할 수 있어야 한다 — 1.4 힌트 규칙.
- 발동 조건이 충족되지 않은 제어는 침묵하는 토글이 아니라 **비활성 + 사유 표기** 상태여야 한다 (EX-05 — 5.5).
- 시나리오가 실데이터를 변동시킬 때는 **무엇이 바뀌었는지 특정**해야 한다 — 인명·슬롯 단위 (EX-04 — 5.4).
- 데모 가이드는 스캔 가능 구조를 따른다 (1.6).

---

## 2. 제품 개요 및 핵심 유저 플로우

### 2.1 문제 정의와 제품 목적 `[PRD-PROBLEM]`

**문제 정의 (사용자 관점):** 여러 명이 회의 시간을 잡는 일의 비용은 교집합 계산이 아니라 **관계 비용**이다. 주최자에게 일정 잡기는 감정노동이다 — 미응답자를 재촉하는 미안함, 누군가의 비선호 시간으로 확정하는 순간 떠안는 결정의 책임, 확정 후 이탈이 발생하면 모든 조율을 처음부터 반복해야 한다는 공포. 참석자에게는 억압이다 — "가능"이라 답하는 순간 그 시간에 박제된다는 부담, "되긴 되는데 그 시간은 좀…" 같은 진심을 단체 채널에서 말하면 까다로운 사람이 된다는 눈치, 그리고 결국 목소리 큰 사람의 조건이 이기는 구조. 기존 도구(메신저 투표, 가능/불가 이분법 조율 서비스)는 계산을 도울 뿐 이 관계 비용을 다루지 않는다.

**제품 목적:** 조율 과정에서 사람이 지불하던 관계 비용을 시스템으로 이관한다. 참석자들의 구조화된 가용성 데이터를 기반으로 최적의 미팅 시간을 자동 도출하는 **가입 없는** 일정 조율 자동화 솔루션이며, 참석자는 회원가입·로그인 없이 초대 링크만으로 응답을 완료한다.

**메커니즘별 사용자 근거:** 본 문서의 모든 핵심 메커니즘은 아래의 사용자 문제에 소급된다. 이 매핑에 소급되지 않는 기능 추가는 장식으로 간주하여 배제한다.

| 메커니즘 | 해소하는 관계 비용 |
|---|---|
| `AVOID` 상태 (3.2) | 까다로운 사람으로 보일 부담 없이 진심을 말하는 통로 — 시스템에 말하는 것의 사회적 비용은 0 |
| 완화 단계·인원 실명 표기 (3.4) | 확정 결정의 책임 — 주최자에게 "제가 정한 게 아니라 조건상 최선"이라는 방어 가능한 근거와, 사전 양해를 구할 대상을 제공 |
| 집계 가시성 뷰 (3.8) | 답만 받고 이해하지 못하는 상태 — 주최자가 동료들의 상황을 직접 확인 |
| 독촉 알림 (5.3) | 재촉의 미안함 — 시스템이 대신 미움받는다 |
| 이동 버퍼 자동 잠금 (5.2) | 본인의 예의로부터 본인을 보호 — 무리한 "갈 수 있어요"를 시스템이 차단 |
| 재조율 플로우 (6) | "처음부터 다시"의 공포 — 기존 응답 데이터를 폐기하지 않는다 |
| 무가입 링크 진입 (2.5-③) | 응답 마찰 — 응답률은 참여 마찰에 반비례한다 |
| 일괄 입력 (3.3) | "아무 때나 괜찮은" 사람의 응답 노동 |
| 명시적 불가 상태 (3.2) | "안 되는 시간을 안 된다고 말할" 확실한 통로 — 미지정은 거부가 아니라 침묵으로 읽힌다 |
| 확정 2탭 확인 (3.4) | 확정은 누군가의 양보를 확정짓는 행위 — 실수 탭이 아니라 인지된 결정이어야 한다 |
| 가용성 재요청 (5.1) | 교착 시의 협상 노동 — "혹시 그 시간 안 될까요"를 시스템이 특정해서 대행 |
| 배제 통합 원칙 (2.7) | "나 없이 정해졌어?"라는 조직적 불신 — 몰래 빠지는 사람이 없도록 선언·통지·가시성을 강제 |
| 참석 취소 선언 (5.5) | 확정 후 "못 가게 됐다"고 말해야 하는 부담 — 취소의 파장을 시스템이 대신 계산하고 전달 |
| 주최자 통합 행동 배너 (2.2-보강) | 여러 카드를 스캔해 스스로 결론 내려야 하는 취합 노동 — 시스템이 결론까지 내리고 승인만 받는다 |
| 프로액티브 재요청 (3.4-보강) | 병목이 공개된 뒤 사후 협상하는 부담 — 실명 공개 전에 조용히 먼저 물어 애초에 눈치볼 상황을 예방 |
| 역강등 요청 (5.1-보강) | "제가 빠질게요"를 주최자에게 직접 말하기 어려운 부담 — 요청 버튼 하나로 그 대화를 대신함 |

### 2.2 핵심 유저 플로우 (Core Flow)

1. **주최자 회의 발의:** 주최자가 회의 제목, 시간 단위(예: 1시간), **조율 기간**, **회의 후보 날짜**를 설정하고 도메인 멤버들의 권한(의사결정자/참조자)을 지정하여 초대 링크를 생성한다(모킹 — 1.1). 발의 완료 시 주최자는 **자신의 대시보드(D01)로 진입**한다 — 참석자 화면으로의 강제 전환을 금지한다(2.5-②).
   - **조율 기간과 회의 후보 날짜의 분리 (v2.5, 발견 77) — `[PRD-PERIOD-SPLIT]` 상세는 3.0-A 참조.** 발의 화면에 두 값을 각각 독립된 입력으로 제공하며, 주최자가 둘 다 자유롭게 지정한다. 회의 후보 날짜의 시작일은 조율 기간 종료일보다 반드시 뒤여야 하며, 날짜 선택 UI 자체가 `min` 제약으로 이를 강제한다(선택 불가능한 날짜는 애초에 고를 수 없게).
   - **발의 후 잠금:** 두 기간 모두 발의 후에는 표시형(읽기 전용)으로 전환한다 — 아래 권한·제목 잠금과 동일 규칙.
   - **주최자 권한 고정 (v1.4):** 주최자 본인(m1)의 권한은 **의사결정자로 고정**하며 토글을 노출하지 않는다. 회의를 발의한 사람이 참조자인 상태를 허용하지 않음으로써, 의사결정자 0명(추천 연산이 전 슬롯을 통과시키는 무의미 상태)의 도달 가능성을 구조적으로 차단한다.
   - **발의 후 권한 잠금 (v1.9):** 권한 토글은 발의 전에만 조작 가능하다. 발의 후 H01 재진입 시 권한은 표시형 뱃지로 잠긴다 — 발의 후의 역할 변경은 통지가 동반되는 강등 플로우(5.1)로만 가능하다. 토글로 통지 없이 필수 인원을 배제할 수 있는 뒷문은 2.7 원칙 위반이다.
   - **발의 후 제목 잠금 (v2.3 — 일관성 수정):** 회의 제목 입력란도 권한 토글과 동일하게 발의 후에는 표시형(읽기 전용)으로 전환한다. 이전까지 제목만 예외적으로 계속 편집 가능했던 것은 잠금 규칙의 일관성이 깨진 결함이었다 — 참석자들이 이미 응답한 회의의 정체성(제목)이 조용히 바뀌는 것을 막는다.
2. **참석자 일정 제출:** 참석자는 회원가입 없이 링크를 통해 진입(초대 랜딩 — 2.5)한 후 본인 인증(모킹 — 1.1)을 거쳐 본인의 외부 캘린더 일정이 연동된 그리드를 확인한다. 이동 버퍼 등이 마스킹된 그리드 위에서 본인의 가용 시간을 편집(섹션 3)하여 최종 제출한다. 제출 직후에는 제출 완료 단계(2.5-④)로 전환된다.
3. **추천 및 최종 확정:** 의사결정자 전원 응답(또는 EX-03 유예 정책) 완료 시 가중치 알고리즘(섹션 3.4)을 통해 최적의 대안 Top 3가 산출된다. 주최자가 대시보드에서 최종안을 선택하면 회의가 확정되며(EX-04 검증 통과 시) 전원의 캘린더에 이벤트가 등록된다(연출 — 1.1). 확정 이후 참석자가 링크로 재진입하면 확정 결과 안내를 본다(2.5-⑤).

### 2.3 컨텍스트 가시성 규칙 `[PRD-CONTEXT]`

데모는 한 브라우저에서 두 역할을 오가므로, 사용자가 "지금 누구의 시점에서 전체 과정 중 어디에 있는지"를 항상 인지할 수 있어야 한다.

**적용 범위:** 역할·단계 표시는 제품 화면(H01·A01·D01·R01)에만 노출한다. 랜딩(L01)과 데모 가이드는 여정 진입 전/외부 화면이므로 노출하지 않는다 (브랜드 바 자체는 전 화면 공통 — 로고 5회 탭 타겟 유지).

**역할 표시:** 제품 화면 상단에 현재 시점을 상시 표기한다.
- H01·D01·R01: `주최자 화면`
- A01: `참석자 화면` (본인 선택 후에는 `참석자 화면 · {이름}`)

**진행 단계 표시:** 제품 화면 상단에 4단계 진행 표시를 상시 노출한다. 단계 도출 규칙 (판정 인원 기준은 **의사결정자** — 3.7):

| 단계 | 조건 |
|---|---|
| ① 발의 | 현재 화면이 H01 |
| ② 응답 수집 | 의사결정자 제출 < 의사결정자 전원 (status PROGRESS, 강제 마감 전) |
| ③ 조율·확정 | 의사결정자 전원 제출(또는 강제 마감) + status PROGRESS |
| ④ 완료 | status COMPLETED (CONFLICT 시 ③으로 회귀 표시) |

### 2.4 과제 브리프 매핑 `[PRD-BRIEF]`

본 제품은 "같은 회사 동료 6명이 다음 주까지 1시간 회의를 잡는 경험 설계" 과제의 응답물이다. 브리프의 조건은 시드 데이터(문서 3)와 아래와 같이 1:1로 매핑되며, 이 매핑은 우연이 아니라 설계다. 시드 수정 시 본 표의 매핑이 깨지면 결함이다.

| 브리프 조건 | 제품 반영 |
|---|---|
| 같은 회사 동료 6명 | 도메인 멤버 6인 시드 (m1~m6), 무가입 링크 진입 |
| 다음 주까지 모여야 함 | 마감 시한 개념 + EX-03 마감 유예 정책 |
| 회의 시간 1시간 | 1시간 슬롯 단위 그리드 (27슬롯) |
| 점심 직후를 피하고 싶은 사람 | m1·m2의 07/14 13:00 `AVOID` |
| 특정 요일 외근이 많은 사람 | m2의 07/14 외근 일정 + EX-02 이동 버퍼 30분 자동 마스킹 |
| 꼭 참석해야 하는 사람 | 의사결정자(REQUIRED) 권한 + EX-03 판정 대상 |
| 선택 참석이어도 괜찮은 인원 | 참조자(OPTIONAL) 권한 + 완화 1단계 제외 대상 + 정렬 1순위 반영 |
| 동료들의 상황을 이해하고 | 주최자 집계 가시성 뷰(3.8) + 추천 카드 완화 근거·인원 실명 표기(3.4) |

### 2.5 여정 완결성 규칙 `[PRD-JOURNEY]`

여정은 시작(랜딩)과 끝(완료 후 순환)이 있어야 하며, 어떤 화면도 "다음 동선 없음" 상태로 사용자를 방치하지 않는다. **역할 간 강제 점프를 금지한다** — 주최자↔참석자 시점 전환은 반드시 사용자가 명시적으로 선택하는 동선(초대 링크 열기, 대시보드 보기)을 통해서만 발생한다.

**① 서비스 랜딩 (L01)**
- 앱 최초 진입 화면. 제품이 해결하는 문제(여럿이 일정 잡기의 번거로움)와 해결 방식(가용성 수집 → 자동 추천 → 확정)을 요약 제시한다.
- 주 액션: [회의 만들기] → H01 진입.
- 하단 소형 링크: 데모 가이드 (1.6).

**② 발의 직후 — 초대 링크 카드 (D01 상단)**
- 발의 완료 시 주최자는 대시보드(D01)로 진입한다. 실제 제품에서 주최자의 다음 행동은 "링크 공유 후 응답 대기"이며, 대시보드가 그 자리다.
- D01 상단에 초대 링크 카드를 노출한다 (노출 조건: status PROGRESS, 하단 노출표와 1:1). 구성: 가상 초대 URL 표기, [링크 복사](복사 완료 토스트 — 연출), **[링크 열어보기]**(→ 초대 랜딩 진입).
- [링크 열어보기]가 주최자 → 참석자 시점 전환의 공식 동선이다. "주최자가 자신이 만든 링크를 직접 열어 확인한다"는 실제 행동의 재현이므로 제품 일루전을 깨지 않는다.
- **응답 유입 (v1.8 → v1.9 개정):** 유입 트리거는 화면 위치가 아니라 **발의 사건**이다(발의 시 launched 플래그 커밋). 주최자가 참석자 화면 등 다른 곳에 있어도 응답은 백그라운드로 유입된다 — 현실의 응답은 주최자가 대시보드를 보고 있을 때만 오지 않는다. 응답 현황이 실시간으로 차오르는 것이 "살아있는 제품"의 재현이다. 유입 도중의 마감 도달 전환·화면 이동·새로고침은 모두 정합해야 한다 (판정은 전부 명단 기반이므로 특수 처리 불요).

**③ 초대 랜딩 (A01 선행 단계)**
- 참석자 화면 진입 시 최초로 노출되는 단계. "링크를 받은 참석자"의 시점을 재현한다.
- 표시: 주최자명, 회의 제목, 소요 시간, 응답 마감 시한. 주 액션: **"가입 없이 바로 응답"** → 본인 선택 단계 진입.
- 무가입 진입이라는 제품 가치(2.1)를 사용자가 체감하는 지점이므로 생략을 금지한다.

**④ 제출 완료 단계 (A01 후행 단계)**
- 가용성 제출 직후 참석자 시점에서 노출된다. **제출 즉시 주최자 화면으로 강제 전환하는 것을 금지한다.**
- 표시 내용: 제출 완료 확인, 현재 응답 현황(n/전원).
- 제공 동선: [가용 시간 다시 수정하기](그리드 복귀 — 3.5 재편집), [다른 참석자로 응답하기](본인 선택 복귀), [주최자 대시보드에서 결과 보기](참석자 → 주최자 시점 전환의 공식 동선 — 데모 페르소나 플로우 1.2).

**⑤ 참석자 결과 안내 — 당사자별 재진입 경로 영구 매트릭스 (v2.3 확장)**
- 참석자 여정의 결말. 확정·재조율·폐기 국면에서 링크로 재진입한 사람이 **자신의 처지에 맞는 화면**을 본다. 배제·변경의 당사자가 일반 안내만 보는 것은 2.7 가시성 요건 위반이다.
- **본 표는 영구 문서다.** 새 회의 상태나 배제 사유가 추가될 때마다 이 표부터 갱신한다 — 재진입 화면의 정의 누락을 사후에 스크린샷으로 발견하는 대신, 신규 케이스 설계 시점에 이 표에 행을 먼저 채우는 것을 규칙으로 삼는다.

| 국면 | 대상 | 화면 |
|---|---|---|
| `PROGRESS` (미발의) | 누구든 | H01로 리다이렉트 (2.6) |
| `PROGRESS` | 일반 참석자 | 정상 플로우 (INVITE→AUTH→GRID→DONE) |
| `PROGRESS` | 강제 마감 제외자 | 일반 플로우 + 배너 "응답 마감으로 현재 추천에서 제외되어 있습니다. 지금 제출하면 즉시 반영됩니다" (지각 제출 — 5.3). **이 배너는 PROGRESS 한정** |
| `PROGRESS` | 강등된 멤버 | 일반 플로우 + 배너에 **강등 근거가 된 슬롯 명시** — "{슬롯}에 참석이 어려우셔서 참조자로 바뀌었어요" (발견 64) |
| `PROGRESS` | 역강등 요청 대기 중인 멤버 (v2.3 신규) | 일반 플로우 + 배너 "참조자 전환을 요청했어요. 주최자님의 확인을 기다리는 중이에요" — 요청 취소 동선 제공 |
| `PROGRESS` | 역강등 요청이 거절된 요청자 (v2.6 신규, 발견 80) | 일반 플로우 + 배너 "요청이 거절됐어요" |
| `PROGRESS` | 필수 복귀 요청 대기 중인 멤버 (v2.6 신규, 발견 81) | 일반 플로우 + 배너 "다시 필수 참석자가 되고 싶다고 요청했어요. 주최자님의 확인을 기다리는 중이에요" — 요청 취소 동선 제공 |
| `PROGRESS` | 필수 복귀 요청이 거절된 요청자 (v2.6 신규, 발견 81) | 일반 플로우 + 배너 "요청이 거절됐어요" |
| `PROGRESS` | 주최자 (역강등 요청 또는 필수 복귀 요청 수신) | 대시보드 상단에 요청 배지 노출, 통합 행동 배너(3.9)에 [승인]/[거절] 포함 |
| `PROGRESS` | candidatePeriod 확장 후, 새로 추가된 날짜에 미응답인 참석자 (v2.6 신규, 발견 79) | 일반 플로우 + 새로 추가된 날짜를 그리드에서 강조 표시, 기존 응답은 그대로 유지 |
| `COMPLETED` | 일반 | 확정 결과 안내(읽기 전용): 확정 일시, 등록 안내(3.4 보정 문구), 불참·미확인 실명 + 하단 링크 **[내 참석 상황을 확인하거나 바꾸고 싶어요]** (→ 5.5 경로 A/C 분기) |
| `COMPLETED` | 미확인자 (경로 C 진입점, v2.2) | 위 링크 클릭 시 본인 목록에서 "아직 답 안 함 · 지금 제출" 표시 → 취소가 아닌 **제출 플로우**로 직행 |
| `COMPLETED` | 대기 중이던 역강등 요청자 (v2.3) | 확정 순간 요청은 **자동 폐기**된다(아래 5.1 규칙) — 재진입 시 일반 확정 결과 안내만 노출, 잔존 요청 상태 없음 |
| `CANCELLED` (v2.3 신규) | 전원 | 읽기 전용 종료 화면 — "이 회의는 취소되었습니다" + 취소 사유(주최자 입력 시). 재제출·재조율 동선 일절 없음 |
| `CONFLICT` | 일반 참석자 | "재조율 진행 중" 안내 + **[내 가용 시간 업데이트하기]** (→ 본인 선택 → 그리드 → 재제출 시 대체안 실시간 재연산) |
| `CONFLICT` (경로 A — 본인 선언) | 취소한 본인 | 일반 참석자와 **동일한 안내 + [내 가용 시간 업데이트하기]** 제공 (v2.0 — 발견 39). 취소는 그 슬롯 하나의 거절이지 연산 제외가 아니므로, 재편집이 실제로 결과에 반영된다 |
| `CONFLICT` (경로 C — 뒤늦은 제출 불일치, v2.2) | 뒤늦게 응답한 본인 | "뒤늦게 응답했는데 이 시간이 맞지 않아"로 명시 — 경로 A(취소)와 다른 문구. 연산 취급은 경로 A와 동일 |
| `CONFLICT` (경로 B — 웹훅 모킹, v2.8 개정) | 이탈자 본인(m3) | 일반 참석자와 **동일한 안내 + [내 가용 시간 업데이트하기]** 제공 (발견 87 — 이전엔 "가용성 업데이트 동선 미제공" 전용 안내였으나, 감지 사유가 있다고 해서 당사자가 직접 새 시간을 알릴 필요가 없어지는 것은 아니므로 폐지). 단, 경로 A(취소 슬롯 UNAVAILABLE 마킹)와 마찬가지로, 이탈 감지 시점에 확정됐던 슬롯은 본인 가용성에서 자동으로 마킹되어(사유: "외부 일정 변동 감지") 동일 슬롯이 재추천되지 않는다 |
| `CONFLICT` | 주최자 (재조율 반영 현황 조회, v2.6 신규, 발견 83) | R01 화면에 "재조율 반영 현황" 카드 노출 — 이탈 시점 이후 가용성을 갱신한 인원과 아직 갱신하지 않은 인원을 구분 표시 (3.8 PROGRESS 제출 현황 카드와 대칭) |
| `CONFLICT` | 주최자 (역할 무관) | **주최자도 의사결정자로 고정된 참석자다** — 경로 A(참석 취소 선언)를 다른 필수 참석자와 동일하게 이용할 수 있어야 한다. 주최자 전용의 별도 취소 기능은 두지 않는다 (v2.3 — 원칙 재확인, 아래 "주최자=참석자" 원칙 참조) |

- **"주최자 = 참석자" 원칙 (v2.3 신규):** 주최자는 의사결정자 역할이 고정된 참석자일 뿐, 별도의 존재가 아니다. 확정 후 주최자 본인의 사정이 바뀌는 경우도 신규 기능이 아니라 **기존 경로 A(참석 취소 선언)를 그대로 적용**한다. 코드·화면 어디에서도 주최자를 이 경로에서 배제해서는 안 된다.
- 가용성 재제출 경로: `PROGRESS`·`CONFLICT`에서만 존재하며, `CONFLICT`에서는 이탈 경로(A·B·C) 무관하게 이탈자 본인을 포함한다(v2.8 개정 — 이전엔 경로 B 이탈자만 예외였다). `COMPLETED`·`CANCELLED`에서 존재하면 결함이다(2.6).

**⑥ 완료 후 순환 — 결과 중심 위계 (v1.8 확대)**
- COMPLETED의 대시보드는 "조율 중" 레이아웃의 재사용을 금지한다. 이 화면의 유일한 1급 정보는 확정 결과다: **확정 카드가 최상단 히어로**(일시 대형 표기 + 등록 안내 + 불참 명시 + 순환 동선), 집계 뷰는 확정 슬롯을 마커로 표시한 "조율 근거 다시 보기"로 기본 접힘, 응답 현황은 한 줄 요약으로 축소한다. 상태별 컴포넌트의 노출 여부는 DS 노출표가, **순서·비중은 DS 위계 규칙**(신설)이 정의한다.
- COMPLETED 상태의 대시보드에는 확정 결과와 함께 [새 회의 만들기] 동선을 제공한다.
- [새 회의 만들기]는 데이터 초기화(7.4)와 동일한 동작 후 랜딩(L01)으로 이동한다 — 데모는 회의 1건만 다루므로, 새 회의 시작은 곧 데모 리셋이다. 이 동작이 커밋 데이터를 삭제함을 실행 전에 안내한다.

### 2.6 화면 진입 가드 `[PRD-ROUTE-GUARD]`

회의 상태에 따라 화면의 진입 가능 여부와 렌더링 상태를 제한한다. 표에 없는 조합으로의 진입 시도(패널 바로가기 포함)는 지정된 화면으로 리다이렉트하거나 지정된 대체 상태를 렌더링한다. 탭 간 동기화(7.6)로 상태가 수신된 경우에도 즉시 동일하게 적용한다.

| 회의 상태 | L01·가이드 | H01 | A01 | D01 | R01 |
|---|---|---|---|---|---|
| PROGRESS (미발의) (v1.9) | 진입 가능 | 진입 가능 | → H01 리다이렉트 | → H01 리다이렉트 | → H01 리다이렉트 |
| PROGRESS (발의 후) | 진입 가능 | 진입 가능 (권한 토글·제목 잠금 — 2.2) | 정상 플로우 | 진입 가능 | 가드 상태 렌더링 (6.5) |
| COMPLETED | 진입 가능 | → D01 리다이렉트 | **확정 결과 안내** 렌더링 (2.5-⑤) | 확정 완료 표기 | 가드 상태 렌더링 (6.5) |
| CONFLICT | 진입 가능 | → R01 리다이렉트 | 당사자별 안내 (2.5-⑤ — v2.8부터 이탈 경로 무관, 이탈자 포함 전원 가용성 업데이트 가능) | → R01 리다이렉트 | 재조율 플로우 |
| CANCELLED (v2.3 신규) | 진입 가능 | → D01 리다이렉트 | 읽기 전용 종료 안내 (2.5-⑤) | 읽기 전용 종료 화면 | → D01 리다이렉트 |

- COMPLETED·CANCELLED 상태에서 확정된 회의에 가용성을 재제출하는 경로가 존재하면 결함이다.
- CONFLICT의 강제 리다이렉트는 **주최자 화면(H01·D01)에만** 적용한다 — 재조율 책임은 주최자에게 있다. 참석자 화면(A01)은 안내 상태로 진입 가능하다.
- CANCELLED는 종결 상태다. 어떤 액션(재제출·재조율·재확정)도 열지 않는다 — 되돌릴 수 없다.
- 데모 가이드 화면은 상태와 무관하게 항상 진입 가능하다 (제품 화면이 아님).

### 2.7 필수 인원 배제의 통합 원칙 `[PRD-EXIT-PRINCIPLE]` (v1.9)

**COMPLETED 불변식:** `COMPLETED`는 언제나 "연산에 포함된 모든 의사결정자가 참석 가능한 시간의 확정"을 의미한다. 시스템은 이 불변식을 깨는 확정을 제공하지 않는다 — '필수'라는 단어가 거짓이 되는 순간 권한 체계(의사결정자/참조자, EX-03 판정, 완화 순서) 전체의 의미가 무너진다. 필수 인원이 못 오는데 회의를 열어야 한다면 답은 둘 중 하나다: 그 사람은 사실 필수가 아니었거나(→ 강등), 회의가 아직 성립하지 않았거나(→ 재요청·재조율).

필수 인원이 연산·성립에서 실제로 빠지는 경로는 강제 마감·강등·이탈 3개이며, 모두 다음 3요소를 갖춰야 한다. (아래 표의 역강등 요청·필수 복귀 요청 행은 배제 자체가 아니라 배제를 촉발/철회하는 **요청** 단계이므로 별도 성격이지만, 같은 통지 원칙이 적용되어 함께 표기한다.)

| 요소 | 의미 |
|---|---|
| ① 명시적 선언 | 시스템이 몰래 빼지 않는다 — 주최자 또는 본인의 확인 영역(3.4 패턴) 통과 |
| ② 실행 전 통지 수단 | 실행 전에 당사자에게 알릴 수단과 그 발송 여부가 확인 영역에 표기된다 (발송 기록은 실데이터 커밋 — 1.1) |
| ③ 사후 가시성 | 당사자가 링크 재진입 시 자신의 처지를 화면으로 인지한다 (2.5-⑤ 당사자별 안내) |

| 경로 | 선언 주체 | 통지 | 사후 가시성 |
|---|---|---|---|
| 강제 마감 (5.3) — 미응답 필수의 배제 | 주최자 | 독촉 알림 (발송 이력 표기) | 제외자 배너 + 현황 "제외" 표기 |
| 강등 (5.1) — 응답했으나 불가한 필수의 참조자 전환 | 주최자 | 가용성 재요청 이력 표기 + 변경 알림 | 강등자 배너 + 현황 (참조), **강등 근거 슬롯 명시** (발견 64) |
| 역강등 요청 (5.1 확장, v2.3 신규) — 본인이 필수 해제를 먼저 요청 | 본인 | 요청 즉시 주최자 배지·통합 배너에 노출 | 요청자 본인은 대기 배너로 확인, 승인 시 강등과 동일 경로, **거절 시 "요청이 거절됐어요" 배너** (v2.6, 발견 80) |
| 필수 복귀 요청 (5.1 확장, v2.6 신규) — 선택 참석자가 필수 복귀를 먼저 요청 | 본인 | 요청 즉시 주최자 배지·통합 배너에 노출 | 요청자 본인은 대기 배너로 확인, 승인 시 attendance REQUIRED 복귀, 거절 시 "요청이 거절됐어요" 배너 (v2.6, 발견 81) |
| 이탈 (5.5) — 확정 후 본인의 참석 취소 | 본인 | (본인 선언이므로 통지 = 주최자 배너) | 이탈자 전용 안내 + 주최자 지속 배너 |

> 서버 부재로 "알림 수신"은 재현하지 않는다 — 당사자 인지는 링크 재진입 시의 화면 상태까지가 데모의 경계다 (1.1, 데모 가이드 명시).
> **v2.6 보강 (발견 80):** 이 표는 지금까지 "요청이 승인되어 실제로 배제가 일어나는" 경로만 다뤘다 — 요청이 **거절**되어 아무 배제도 일어나지 않는 경우의 통지 요건이 누락되어 있었다. 배제가 일어나지 않는다고 통지 의무가 사라지는 것은 아니다: 요청자는 자신의 요청이 어떻게 처리됐는지 알아야 한다. 승인/거절 양쪽 다 사후 가시성을 요구한다.
> **회의 폐기(CANCELLED)는 이 원칙의 적용 대상이 아니다.** 배제 원칙은 "일부 인원이 연산에서 빠지는" 사건을 다루지만, 폐기는 회의 자체가 종료되는 것이라 전원에게 동일하게 적용된다 — 아래 [PRD-CANCEL-MEETING] 참조.

---

## 3. 그리드 편집 명세 (해피 패스) `[PRD-GRID]`

본 제품의 핵심 인터랙션. 예외 처리(섹션 5)보다 본 섹션이 우선 구현 대상이다.

### 3.0-A 조율 기간과 회의 후보 날짜의 분리 `[PRD-PERIOD-SPLIT]` (v2.5 신설)

- **두 개념의 정의:**
  - **조율 기간 (`coordinationPeriod`):** 이 제품을 사용해 회의 후보 날짜를 정해가는 실시간 진행 구간이다. 참석자 응답 수집, 재요청·강등 협상이 이 기간 동안 일어난다. **조율 기간의 종료 시각이 곧 응답 마감이다** — 별도의 `deadline` 필드는 두지 않는다(1.3).
  - **회의 후보 날짜 (`candidatePeriod`):** 조율이 끝난 뒤 회의가 실제로 열릴 수 있는 날짜 범위. 참석자가 그리드에서 "가능/불가능"을 표시하는 대상이자, 대시보드·히트맵에 노출되는 날짜들이다.
- **분리 원칙:** 두 기간은 **절대 겹칠 수 없고**, 회의 후보 날짜의 시작일은 반드시 조율 기간의 종료일보다 뒤여야 한다. 아직 조율이 진행 중인(즉 참석자들이 여전히 응답을 내고 있는) 바로 그 날짜를 동시에 "회의가 열릴 수 있는 후보"로 제시하는 것은, 아직 결론 나지 않은 것을 이미 확정된 선택지처럼 보여주는 모순이다.
- **입력 방식:** 두 값 모두 **주최자가 각각 자유롭게 지정**한다(2.2). 시스템은 어느 한쪽을 보고 다른 쪽을 자동으로 계산해 채워주지 않는다 — 다만 회의 후보 날짜의 날짜 선택 UI는 조율 기간 종료일 이전을 `min` 제약으로 원천 차단해, 애초에 무효한 값을 고를 수 없게 한다.
- **재조정 시 규칙:** 주최자가 조율 기간을 나중에 더 늦게 조정해서 이미 선택된 회의 후보 날짜가 무효해지면(겹치거나 역전되면), 시스템이 후보 날짜를 임의로 밀어주지 않는다. 대신 후보 날짜 입력을 **비우고** "조율 기간이 바뀌어 회의 후보 날짜를 다시 선택해주세요" 안내만 표시한다 — 최종 선택은 끝까지 주최자의 몫이다.
- **조기 확정과의 관계:** 필수 참석자 전원이 조율 기간 중 일찍 응답을 마치면, 조율 기간 종료(마감)를 기다리지 않고 즉시 추천·확정이 가능하다(3.7) — 이 규칙은 v2.5 이전부터 있던 동작이며 이번 분리로 달라지지 않는다.
- **표시 정렬 (v2.7 신설, 발견 85):** H01의 조율 기간·회의 후보 날짜 두 카드는 라벨과 값(입력 필드 포함) 모두 **가운데 정렬**한다. 1.7-A 정렬 일관성 규칙(카드 본문은 기본 좌측 정렬, CF01만 예외)의 **추가 예외**로 명시한다 — 이 두 카드는 발의 전 주최자가 가장 먼저 마주치는 핵심 설정값이라 시각적으로 강조할 필요가 있어, CF01과 같은 근거로 예외를 둔다.
- **회의 후보 기간 최대 길이 (v2.9 신설, 발견 88):** 회의 후보 날짜(`candidatePeriod`)는 **최대 7일**을 넘을 수 없다. 근거: 실무 데이터상 일회성 회의의 90%가 발의로부터 10일 이내에 성사되고, 일주일 이상 전에 예약되는 경우는 8%에 불과하다(2025~2026 Flowtrace 조사) — 조율 기간(응답 수집)까지 포함한 전체 소요를 10일 안쪽으로 맞추려면 후보 날짜 쪽 상한은 7일이 합리적이다. 날짜 선택 UI의 종료일 입력은 `max` 제약(시작일 + 6일)으로 이를 강제한다 — 그 이상은 애초에 선택할 수 없다.
- **후보 기간 확장 시 자동 유입 데이터의 정합성 (v2.9 신설, 발견 88):** 배경 응답 유입 시뮬레이션(2.5-②, `[DEV-ARRIVAL]`)이 생성하는 가짜 멤버 응답은 **그 회의의 실제 `candidatePeriod` 전체**를 기준으로 채워야 한다. 고정된 날짜 수(예: 3일)로 시드를 생성하고 나머지 날짜를 빈 채로 남겨두면서도 해당 멤버를 SUBMITTED로 표시하는 구현은 결함이다 — 주최자가 후보 기간을 3일보다 길게 잡은 모든 경우에 "일부 날짜는 아무도 응답 안 했는데 전원 제출 완료로 표시됨" 오류로 이어진다. 이 규칙은 발의 후 기간 확장(`extendPeriod`, 5.1)에도 동일하게 적용된다 — 이미 `periodExtendedFrom`/`extensionUpdatedIds`(v2.7 발견 86)로 확장 이후의 갱신 여부는 추적하고 있었으나, 애초에 발의 시점(H01)에 3일보다 긴 후보 기간을 선택하는 경로에는 이 보호가 적용되어 있지 않았다.

### 3.1 그리드 구조

- 회의 후보 날짜(`candidatePeriod`): 주최자가 발의 시 설정한 날짜 범위이며, **조율 기간(응답 마감) 이후**여야 한다(3.0-A). 데모 시드는 조율 기간 **2026-07-13 ~ 2026-07-15 18:00** 이후인 **2026-07-16 ~ 2026-07-18 (3일)**을 기본 후보 날짜로 둔다.
- 하루 표시 시간대: **09:00 ~ 18:00**
- 슬롯 단위: 회의 시간 단위와 동일 (시드 기준 1시간 → 하루 9슬롯 × 3일 = 27슬롯)
- 축 구성: 가로축 날짜, 세로축 시간 (모바일 세로 스크롤 기준)
- 그리드가 렌더링하는 날짜 범위는 `candidatePeriod`이지 `coordinationPeriod`가 아니다 — 혼동 시 결함이다.

### 3.2 슬롯 상태 정의

| 상태 | 의미 | 유저 변경 |
|---|---|---|
| `UNSET` | 미지정 (초기값) | 가능 |
| `AVAILABLE` | 가용 | 가능 |
| `AVOID` | 비선호 (가능하지만 피하고 싶음) | 가능 |
| `UNAVAILABLE` | **유저 지정 불가** — "이 시간은 안 됩니다"의 명시적 선언 (v1.8) | 가능 |
| `BLOCK_STRICT` | 시스템 잠금 (기존 일정 + EX-02 이동 버퍼) | **불가** |

- `UNSET`은 연산 시 **가용 불가**로 간주한다 (보수적 판정).
- `UNAVAILABLE`은 연산상 `UNSET`과 동급(어떤 완화 단계에서도 격상 불가)이나, **표기상 반드시 구분한다** — 주최자에게 "확실히 안 되는 사람"(불가)과 "응답하지 않은 구역"(미확인)은 판단 근거가 다르다. 시각적으로도 시스템 잠금과 구분한다 (본인이 지정했고 본인이 해제 가능).
- `BLOCK_STRICT` 슬롯은 어떤 입력에도 반응하지 않으며, 유저가 **출처를 인지할 수 있어야 한다** (v1.9 — 발견 28): ① 그리드 하단에 상시 캡션 "회색 잠금은 연동된 캘린더의 기존 일정입니다 (이동 버퍼 포함, 자동 반영)" ② 잠금 슬롯 탭 시 토스트에 실제 일정명 표기 (예: "외근 (고객사 방문) · 이동 버퍼 30분"). 사용자가 만들 수도 풀 수도 없는 상태의 출처를 설명하지 않는 것은 결함이다.

### 3.3 입력 규칙

- **탭(클릭):** 해당 슬롯 상태를 순환 전환한다. `UNSET → AVAILABLE → AVOID → UNAVAILABLE → UNSET` (v1.8 — 4상태 순환)
- **드래그:** 드래그 시작 슬롯에서 전환될 다음 상태를 "적용 상태"로 확정하고, 드래그가 지나가는 모든 슬롯에 그 적용 상태를 일괄 도포한다. (시작 슬롯이 UNSET이면 지나가는 슬롯 전부 AVAILABLE로)
- 드래그 중 `BLOCK_STRICT` 슬롯은 건너뛴다 (에러 없이 무시).
- **일괄 입력 (v1.5):** 그리드 상단에 [나머지 모두 가능] 액션을 제공한다. 실행 시 `UNSET` 슬롯만 일괄 `AVAILABLE`로 전환하며, 이미 지정한 `AVOID`·`UNAVAILABLE`·`BLOCK_STRICT`는 보존한다 — 불가·비선호를 먼저 찍고 나머지를 일괄 가능 처리하는 실제 응답 순서와 일치한다 — "아무 때나 괜찮다"는 유저의 응답을 탭 1회로 끝낸다.
- **일 단위 일괄 불가 (v2.3 신설):** 날짜 헤더를 길게 누르거나 탭하면 "이 날 종일 안 돼요" 액션이 뜬다. 실행 시 해당 날짜의 `UNSET` 슬롯을 일괄 `UNAVAILABLE`로 전환한다(기존 지정값 보존, 위 규칙과 대칭). 실제 응답 패턴이 "이번 주 화요일은 아예 못 나온다"처럼 날짜 단위인 경우가 많다는 데 근거한다 — 27개 슬롯을 하나씩 탭하는 부하를 줄인다.
- 편집 중 데이터는 임시 상태로만 존재한다 (섹션 7).

### 3.4 제출 및 추천 연산 정책

**제출 가능 조건:** `AVAILABLE` 슬롯이 1개 이상 존재해야 한다. 미충족 시 제출 액션은 비활성화되며 사유를 안내한다.

**추천 연산 (Top 3 산출):**
1. **기본 연산:** 응답한 전원(의사결정자 + 참조자)이 `AVAILABLE`인 슬롯을 탐색
2. **완화 1단계 (기본 연산 결과 0개 시):** 참조자를 제외하고 의사결정자 전원 `AVAILABLE` 슬롯을 재탐색 — `[PRD-EX-01]`
3. **완화 2단계 (1단계도 0개 시):** 의사결정자의 `AVOID`를 가용으로 임시 격상하여 재탐색 — `[PRD-EX-01]`
4. **완화 3단계 — 부분 성립 (2단계도 0개 시) (v1.4):** 의사결정자 전원 성립을 포기하고, 슬롯별로 참석 가능(`AVAILABLE` 또는 `AVOID`)한 의사결정자 수를 집계하여 **가장 많은 의사결정자가 참석 가능한 슬롯**을 제시한다. 결과 카드에는 참석 불가로 예상되는 의사결정자의 이름을 명시한다 (예: "1명 참석 불가: 이디자"). — `[PRD-EX-01]`

**정렬 규칙 (동일 단계 내) — v2.1 우선순위 역전 (발견 49):**
- 1순위: `AVOID`로 참여하는 필수 참석자 수가 적은 순 (완화 3단계에서는 참석 가능 의사결정자 수가 많은 순을 최우선으로)
- 2순위: 선택 참석자 중 `AVAILABLE`인 인원 수가 많은 순
- 3순위: 빠른 시간 순
- **역전 근거:** v2.0까지는 "선택 참석자 참여 인원"을 1순위로 뒀으나, 이는 정작 핵심인 "필수 참석자의 불편 최소화"를 밀어내는 결과를 낳았다 — 필수 1명이 피하고 싶어하지만 선택 참석자가 많이 오는 시간이, 아무도 피하지 않지만 선택 참석자가 없는 시간보다 상위에 뜨는 것은 관계 비용 최소화라는 총론과 모순된다. 필수 참석자의 불편을 줄이는 것이 항상 우선한다.
- **투명성 (v2.1, 선택 적용):** Top3 카드에 [왜 이 순서인가요?] 펼침을 제공해 정렬 근거를 한 줄로 노출할 수 있다 (예: "피하고 싶어하는 사람이 가장 적어서"). 총론의 "근거를 숨기지 않는다" 원칙의 연장이나, 필수 구현 항목은 아니다.

**결과 카드 표기:** 각 추천안에는 어떤 완화 단계로 산출되었는지 표기한다 (예: "전원 가능" / "참조자 1명 제외" / "비선호 시간 포함" / "1명 참석 불가"). 유저에게 산출 근거를 숨기지 않는다.
- **서브라인 규칙 (v1.8):** 서브라인은 라벨 문구를 반복하지 않고 인원 정보만 담는다 — 완화 2단계: "비선호로 참여: {이름들}" / 부분 성립: "참석 불가: {명시 불가·외부 일정 인원} · 미확인: {미지정 인원}". 부분 성립 서브라인은 **명시적 불가와 미확인을 반드시 구분**하고, 외부 일정(시스템 잠금)에 의한 불가는 사유를 병기한다 (5.1 재요청 분기의 근거).
- **인원 실명 표기 (v1.5):** 완화가 특정 인원의 양보를 전제하는 경우 그 인원의 이름을 카드에 명시한다. 주최자가 확정 전에 **누구에게 사전 양해를 구해야 하는지** 알게 하는 장치다(2.1). 익명 집계만 표기하는 것을 금지한다.

**불참 사유 3분기 표기 `[PRD-ABSENCE-REASON]` (v2.1 — 발견 48):** 연산·확정 결과에서 빠진 인원을 표기할 때는 반드시 사유를 구분한다 — "빠졌다"는 사실 하나로 뭉뚱그리면 서로 다른 세 상황이 같은 문구("불참 예상")로 보이는 오해가 생긴다.

| 사유 | 의미 | 표기 |
|---|---|---|
| 미확인 | 마감까지 응답하지 않아 강제 마감으로 배제됨 — 본인 의사 없음 | "미확인 (마감까지 답 없었음): {이름}" |
| 참석 불가 | 응답은 했으나 해당 슬롯이 안 됨(비선호·불가·잠금) — 데이터 근거 있음 | "참석 불가: {이름}" (기존 부분 성립 서브라인 규칙과 동일) |
| 자발적 불참 | 선택 참석자가 경로 A(5.5)로 본인이 명시적으로 선언 | "불참 알림: {이름}" |

- 완료 화면과 확정 확인 문장 모두 이 구분을 따른다. 강제 마감으로 빠진 인원을 자발적 불참과 같은 문구로 표기하는 것은 결함이다.
- 확정 확인 문장(아래 확정 확인 규칙)에 강제 마감 배제자가 있는 경우, "{이름}님은 답이 없어 포함되지 않았습니다"를 사유와 함께 명시한다 — 라벨("응답자 전원 가능")만으로는 이유가 드러나지 않으므로 문장으로 보강한다.

**확정 확인 규칙 (v1.8 → v1.9 재설계):** 확정은 누군가의 양보를 확정짓는 관계 비용이 걸린 결정이므로, 모든 [최종 확정]은 **확인 영역**을 거친다. v1.8의 armed 버튼 패턴(버튼 문구 변형 + 4초 자동 소멸)은 폐기한다 — 사라지는 UI는 불안을 만들고, 취소 수단이 암묵적이다.
- 1탭 시 해당 카드 하단이 확인 영역으로 확장된다: **문장형 설명 + [취소] + [이 시간으로 확정]** 명시 버튼 2개. 자동 소멸 없음, 다른 카드 조작 시에만 접힘.
- 설명 문장에는 양보 내용을 실명·결과와 함께 담는다:
  - 전원 가능: "전원이 참석 가능한 시간입니다."
  - 비선호 포함: "김주최, 이디자님이 비선호하는 시간입니다. 확정 전 양해를 구하는 것이 좋습니다."
- 본 확인 영역 패턴은 확정 외에도 무게 있는 결정 전반(강등 5.1, 강제 마감 5.3, 참석 취소 5.5, 새 회의 만들기)에 공통 적용한다.

**등록 문구 보정 규칙 (v1.8):** 확정 완료 안내의 "전원 캘린더에 등록되었습니다"는 전원이 참석 가능할 때만 쓴다. 불참 예상·미확인 인원이 있으면 **"참석 가능 인원(n명)의 캘린더에 등록되었습니다"**로 보정하고 불참·미확인 인원을 실명 명시한다 — 연산 라벨 보정("전원"→"응답자 전원")과 같은 원칙이다.
- **보정 규칙:** 연산에 포함되지 않은 인원(강제 마감으로 제외된 미응답자, 미응답 참조자 — 3.7)이 1명 이상 존재하면 "전원 가능" 대신 **"응답자 전원 가능"**으로 표기한다. 연산에 포함되지 않은 인원이 있는데 "전원"이라 표기하는 것을 금지한다.

### 3.5 재편집 규칙

- 이미 제출(SUBMITTED)한 멤버를 다시 선택하면, **기존 제출 데이터를 편집 버퍼에 로드한 상태**로 그리드에 진입한다. 빈 그리드로 시작하는 것을 금지한다.
- 재제출 시 기존 커밋을 덮어쓴다 (localStorage 갱신, 멤버 상태는 SUBMITTED 유지).
- 마감 도달(가상 시계) 이후의 재편집 제한은 두지 않는다 — 데모 스코프에서 제외.
- 재편집은 status `PROGRESS`에서만 가능하다. `COMPLETED`·`CONFLICT`에서는 A01이 결과 안내 상태로 렌더링되므로(2.6) 그리드 진입 자체가 불가하다.

### 3.6 그리드 화면 이탈 수단

- 그리드 편집 화면에는 본인 선택 단계로 돌아가는 수단(뒤로가기)을 상시 제공한다.
- 뒤로가기 시 편집 중이던 임시 데이터는 폐기하지 않고 버퍼에 유지한다 (섹션 7.5와 동일 원칙).

### 3.7 참조자 미응답 판정 규칙

미응답 인원의 판정 기준을 역할별로 분리한다. 모든 판정(결과 산출 차단, 마감 유예, 진행 단계)의 인원 기준은 **의사결정자**로 통일한다.

- **의사결정자 미응답:** 결과 산출을 차단하고(Top 3 미노출), 마감 도달 시 EX-03 유예 정책 대상이 된다.
- **참조자 미응답:** 결과 산출을 차단하지 않는다. 해당 참조자는 연산에서 제외되며, 응답 현황 표기에만 미응답으로 남는다. EX-03 배너 대상이 아니다.
- 참조자 미응답 상태로 산출된 결과에는 3.4 보정 규칙("응답자 전원 가능")이 적용된다.
- 진행 단계 판정(2.3)도 의사결정자 전원 제출을 기준으로 한다 — 결과가 노출되는데 단계는 "응답 수집"에 머무는 표시 모순을 금지한다.

### 3.8 주최자 집계 가시성 `[PRD-HEATMAP]` (v1.5)

과제 브리프의 요구 "사용자가 **동료들의 상황을 이해하고**"에 대응하는 뷰. 주최자에게 답(Top 3)만 주고 근거 데이터를 숨기면, 주최자는 "화요일 오전은 왜 안 되는지"를 알 수 없고 추천을 신뢰할 근거도 없다.

- **위치·형태:** D01의 보조 뷰. 기본 접힘 상태의 [전체 가용 현황 보기] 확장 영역 — 답 우선, 이해는 선택이라는 위계를 유지한다.
- **내용:** 그리드와 동일한 27슬롯 배열에, 슬롯별 **참석 가능 인원 수(절대값)**를 농도로 표기한 읽기 전용 집계(히트맵). 슬롯 선택 시 해당 슬롯의 인원별 상태를 이름과 함께 표시한다.
- **밀도 규칙 (v1.9 — 발견 29):** 비율 밴드가 아니라 **인원수 1명 = 1단계**의 절대 램프를 쓴다 (0명~6명 = 7단계). 4밴드 비율 방식은 6인 기준 4명과 5명이 같은 색이 되어 "이 시간이 더 유력하다"는 비교 판단(이 뷰의 존재 이유)을 파괴한다.
- **마커 분리 (v1.9 — 발견 30):** 확정된 슬롯의 상시 마커(굵은 강조 링 + 상세의 "확정된 시간" 라벨)와 사용자의 탐색 선택 링(얇은 중립 링)은 시각적으로 구분한다. 같은 표현을 쓰면 확정 시간이 어디였는지 탐색 중에 소실된다.
- **색상 계열 분리 (v2.0 — 발견 41):** 확정 슬롯은 후보 슬롯들의 밀도 램프(같은 색조의 명도 단계)와 **아예 다른 색상 계열**의 배경을 쓴다 — "후보"가 아니라 "결정된 사실"이라는 정보 종류의 차이가 색만으로 즉시 읽혀야 한다.
- **명도 스텝 확대 (v2.0 — 발견 40):** 저농도 구간(1~3명)이 배경·서로와 잘 구분되지 않는 문제가 있었다. 6단계 전체를 더 넓은 명도 폭에 재배치해 인접 단계가 육안으로 분명히 갈리도록 한다.
- **집계 기준:** 제출 완료 멤버만 집계한다. 의사결정자와 참조자는 시각적으로 구분한다 (연산 가중치와 일치 — 3.4).
- **편집 불가:** 이 뷰에서 타인의 가용성을 수정하는 경로를 제공하지 않는다 — 조작이 아니라 이해의 도구다.

### 3.9 주최자 통합 행동 배너 `[PRD-NEXT-ACTION]` (v2.3 신설, v2.5 범위 축소, v2.6 대상 확장)

- **문제:** 지금까지 주최자는 배너·현황·히트맵·Top3 카드를 각각 훑으며 "지금 뭘 해야 하는지"를 스스로 조합해야 했다 — 데이터는 다 있는데 판단은 여전히 사람 몫이었다.
- **해결:** D01 대시보드 최상단에 **승인/거절이 필요한 요청 전용** 배너를 둔다. 대기 중인 역강등 요청이 있으면 "{이름}님이 참조자 전환을 요청했어요 (사유: {reason})" + [승인]/[거절]을, 대기 중인 필수 복귀 요청(v2.6, [PRD-REINSTATE-REQUEST])이 있으면 "{이름}님이 다시 필수 참석자가 되고 싶다고 요청했어요 (사유: {reason})" + [승인]/[거절]을 노출한다. 둘 다 없으면 배너 자체가 안 뜬다. 두 요청이 동시에 대기 중인 경우 각각 별도 배너로 노출한다(하나로 뭉뚱그리지 않는다 — 유형이 다르면 승인 결과도 다르다).
- **v2.5 범위 축소 (발견 74):** v2.3에서는 이 배너가 "재요청 시 즉시 전원 합의 가능" 케이스까지 요약해서 보여줬는데, 이게 Top3 카드 안의 재요청 액션과 완전히 같은 내용을 중복 노출하는 결함이었다. 재요청은 **Top3 카드에서만** 노출한다 — 카드가 이미 슬롯별 맥락(누가·왜 병목인지)을 정확히 보여주므로 상단 요약은 불필요한 중복이었다. EX-03(미응답 필수 참석자) 배너도 기존 대로 자기 자리(5.3)에서 노출되며 이 통합 배너에 흡수되지 않는다.
- 이 배너는 강등 CF01(5.1)의 승인 경로와 동일한 처리(demoteMember 재사용)로 이어진다.

---

## 4. 회의 상태 머신 `[PRD-STATE]`

```
PROGRESS ──(주최자 확정 + EX-04 검증 통과)──> COMPLETED
COMPLETED ──(EX-05 이탈 감지)──> CONFLICT
CONFLICT ──(재조율 플로우에서 대체안 재확정)──> COMPLETED
(PROGRESS | COMPLETED | CONFLICT) ──(주최자 회의 폐기)──> CANCELLED   [v2.3 신규]
```

- `PROGRESS`: 조율 진행 중 (기본값)
- `COMPLETED`: 확정 완료. **EX-04 토글이 OFF인 경우 확정 액션은 반드시 이 상태에 도달해야 한다** (도달 불가능한 상태로 남겨두는 것 금지)
- `CONFLICT`: 확정 후 성립 조건 붕괴. 재조율 플로우로만 빠져나갈 수 있다.
- `CANCELLED` (v2.3): 회의 자체의 종료. **어떤 상태에서든 진입 가능하고, 어떤 상태로도 되돌아갈 수 없는 종결 상태**다 — 상태 머신의 유일한 흡수 상태(absorbing state). 상세: [PRD-CANCEL-MEETING] 5.6.
- 멤버 상태는 별도로 `PENDING → SUBMITTED` 단방향 전이만 존재한다.
- **모든 상태 전이는 발생 즉시 localStorage에 커밋한다 (7.2). 새로고침으로 전이가 되돌아가는 구현은 결함이다.**

---

## 5. 글로벌 크리티컬 예외 처리 정책 `[PRD-EXCEPTION]`

### 5.1 [PRD-EX-01] Deadlock: 가용 시간 전멸 정책

- **발생 상황:** 모든 참석자가 가용 일정을 제출했으나, 조건을 충족하는 교집합 타임슬롯이 0개인 경우.
- **해결 규칙:** 시스템은 어떤 데이터 조합에서도 빈 결과 화면이나 에러 문구를 노출하지 않는다. 섹션 3.4의 완화 1단계 → 2단계 → **3단계(부분 성립)** 알고리즘을 순차 적용해 차선책 카드를 반드시 생성한다. 완화 3단계는 정의상 항상 1개 이상의 결과를 산출하므로(슬롯별 집계는 공집합이 될 수 없음), "결과 없음" 상태는 시스템에서 구조적으로 제거된다. 완화 단계는 결과 카드에 명시한다.
- **교착 해소 플로우 (v1.8 → v1.9 전면 개정) `[PRD-DEADLOCK-FLOW]`:** 부분 성립은 답의 끝이 아니라 협상의 시작이다. 2.7 불변식에 따라 **불참을 전제한 강행 확정은 존재하지 않는다** (v1.8의 '불참 확정' 폐기). 부분 성립 카드의 출구는 다음과 같으며, 위계는 이 순서다:
  1. **[가용성 재요청] — 주 액션.** 시스템이 병목을 특정해 협상을 대행한다: "{이름}님이 {슬롯}을 열어주면 전원 참석이 가능합니다" + [재요청 보내기]. 발송 기록은 커밋되고(⑧), **발송 후 버튼은 "재요청 보냄 ✓" 상태로 전환**된다 — 보냈는지 알 수 없는 무상태 버튼은 결함이다 (발견 31). 병목이 해소되면(재편집 반영) 카드 자체가 상위 완화 단계로 대체되므로 상태는 자연 초기화된다. 병목이 시스템 잠금(외부 일정)이면 재요청 대신 "{이름}님은 해당 시간에 외부 일정이 있습니다"를 안내한다.
  2. **[{이름}님을 참조자로 변경하고 진행] — 보조 액션 (강등).** 노출 조건: 해당 슬롯의 불참 예상 **의사결정자가 정확히 1명**일 때만. 확인 영역(3.4 패턴)에 다음을 표기한다: ① 재요청 발송 이력 — 미발송이면 "아직 가용성 재요청을 보내지 않았습니다" 경고 + [재요청 먼저 보내기] 보조 버튼, 발송했으면 "재요청 발송함 ✓" ② 결과 문장: "{이름}님을 참조자로 변경합니다. 이 회의는 {이름}님의 참석 없이 성립하며, 회의 내용을 별도로 공유해야 합니다. 확정 시 {이름}님에게 변경 알림이 전송됩니다." ③ **의견 남기기 (선택, v2.3 신설):** "회의 전 남기고 싶은 의견이 있으신가요? (선택)" 텍스트 입력 — 강등되는 사람이 완전히 사라지는 게 아니라 참여 방식이 바뀌는 것임을 실질적으로 구현한다. **가시성 요구사항 (v2.5 발견 76, v2.6 발견 82로 범위 정정):** 입력받는 것만으로는 부족하다 — 남긴 의견은 강등 당사자의 PROGRESS 재진입 배너("주최자님의 메모: {의견}")에 실제로 노출되어야 한다. 입력 필드만 만들고 값을 아무 데도 안 보여주는 구현은 결함이다. **주최자 본인의 확정 완료 화면(D01-DONE)에는 노출하지 않는다** — 그 메모는 주최자가 강등 당사자에게 남긴 것이지 주최자 자신에게 남긴 것이 아니므로, 주최자 화면에 재노출하는 것은 자신이 쓴 글을 자신에게 다시 보여주는 무의미한 구조다(발견 82). v2.5 문서가 "확정 완료 카드 양쪽에" 노출된다고 잘못 명시했던 것을 여기서 정정한다. ④ [취소] / [참조자로 변경]. 실행 시 권한 변경 커밋(⑦) + 변경 알림(연출) + 의견 저장(있는 경우) → 추천이 즉시 재연산되어 해당 슬롯은 "참조자 제외" 계열 카드로 승격 → 일반 확정 플로우.
  3. **[기간 넓혀서 다시 찾기] — 보조 액션, 사람을 건드리지 않는 대안 (v2.3 신설, v2.5 대상 명확화, v2.6 통지 보강).** 강등·재요청과 나란히 노출한다. 실행 시 **회의 후보 날짜(`candidatePeriod`)를 확장**(데모에서는 고정폭 연장 — 1.1 모킹 표)하고 동일 알고리즘을 다시 돌린다. 조율 기간(`coordinationPeriod`)은 이 액션과 무관하다 — 넓히는 대상은 항상 "회의가 열릴 수 있는 날짜"이지 "조율이 진행되는 기간"이 아니다. 관계 비용 관점에서 사람을 대상으로 한 협상(재요청)이나 지위 변경(강등)보다 마찰이 적은 경로이므로, 사용자가 먼저 시도해볼 만한 선택지로 제시한다.
     - **통지 요구사항 (v2.6, 발견 79):** 이 액션은 [재요청]과 무관한 별개 기능이다 — 재요청은 병목 1인을 대상으로 하지만, 기간 확장은 새로 추가된 날짜에 대해 **전원이 미응답 상태**가 되는 사건이다. 실행 즉시 전원에게 "회의 후보 날짜가 늘어났어요, 늘어난 날짜도 확인해주세요"를 알린다. 대상이 1명이 아니라 전원이라는 점에서 기존 재요청 알림과 구분되는 별도 케이스다. 서버가 없으므로 실제 발신은 하지 않되(1.5·2.7 데모 경계 원칙 동일 적용), 참석자가 링크 재진입 시 이 사실을 화면으로 인지해야 한다(2.5-⑤).
     - **참석자 화면 반영:** 기존 응답은 그대로 유지하고, **새로 늘어난 날짜만 강조 표시**하여 그 부분만 추가 입력하면 되도록 한다. 그리드 전체를 재입력하게 만드는 구현은 결함이다 — 이미 답한 날짜를 다시 만지게 하는 것은 불필요한 반복 노동이다.
     - **신규 날짜 초기 상태 (v2.7 신설, 발견 86):** 늘어난 날짜는 **누구에게도 미리 채워진 값이 없는 빈 상태(미지정)**로 시작해야 한다. "기간 확장 시 즉시 실질적 결과가 나오게" 미리 채워두는 식의 편의적 시드 데이터는 발견 79의 취지(전원이 새 날짜에 실제로 응답해야 한다는 사실 자체가 알림의 핵심 내용) 자체와 모순된다 — 안 채워도 이미 다 채워져 있으면 통지가 무의미해진다. 시연 편의를 위한 사전 채움은 이 범위에서 전면 금지.
     - **제출 현황 재대기 (v2.7 신설, 발견 86):** 기간 확장 실행 즉시, 주최자 대시보드의 제출 현황 카드는 **다시 대기 상태**를 반영해야 한다. 기존에 이미 제출을 마쳤던 참석자라도 새 날짜에 대해서는 아직 응답한 적이 없으므로, "전원 제출 완료"처럼 보이는 표시는 사실과 다르다. 각 참석자가 그리드에 재진입해 새 날짜 부분을 실제로 채워 제출해야만 그 사람 몫이 "완료"로 바뀐다. 이 판정은 기존 "최초 제출 여부"(대기→제출완료, 영구 유지)와는 별개의 축이다 — 발견 83의 "이탈 후 갱신 여부" 판정과 동일한 성격의 문제이며, 같은 패턴(추가 추적 필드)으로 해결한다.
  4. **불참 예상 필수가 2명 이상인 카드:** 강등을 제안하지 않는다 — "필수 참석자 2명 이상이 불가한 시간입니다. 재요청하거나 다른 시간을 검토하세요." 다수 강등은 회의의 성립 자체를 의심해야 할 신호이지 원탭 결정이 아니다. [기간 넓혀서 다시 찾기]는 이 경우에도 노출한다.
  5. **주최자 본인이 병목인 경우:** 주최자는 강등 불가(2.2 고정)이므로 "주최자 본인의 가용 시간을 조정해야 합니다" 안내 + 본인 재편집 유도.
  - **해소 루프의 실구현:** 재요청 대상의 재편집(3.5) 또는 강등으로 병목이 풀리면 추천이 즉시 상위 완화 단계로 복귀해야 한다 — 협상→해소 사이클 전체가 서버 없이 실동작하는 것이 본 데모의 핵심 증명이다.
  - 강등 이력(원래 필수였다는 기록)의 화면 표기는 스코프 밖(8) — 실서비스에서는 변경 로그가 필요한 사안이다.
  - **왜 "회의 분할"은 채택하지 않았는가:** 필수 인원을 두 그룹으로 나눠 각자 되는 시간에 확정하는 방식을 검토했으나 기각했다 — 회의는 "같은 정보를 같은 순간에 같이 보고 그 자리에서 결정하는 것"이 본질이라, 그룹을 나누면 회의가 아니라 서로 다른 두 번의 미팅이 되고 "나만 그 자리에 없었다"는 소외·"누구 결정이 최종인가"라는 혼란을 새로 만든다. 관계 비용을 없애는 게 아니라 다른 모양으로 옮기는 것이므로, 강등·재요청·기간 확장 세 경로로 충분하다고 판단했다 (v2.3 검토 결론).

**역강등 요청 `[PRD-PROMOTE-REQUEST]` (v2.3 신설):** 강등은 지금까지 주최자가 시작하는 일방향이었다. 실무에서는 참석자 본인이 먼저 "저는 이 회의에 꼭 필요하지 않은 것 같아요"라고 말하는 경우가 더 흔하다 — 이 역방향 요청 경로를 만든다.
- **진입:** 그리드 편집 화면 또는 제출 완료 화면에서 필수 참석자만 [저는 꼭 필요한 사람이 아닌 것 같아요] 요청 가능. 주최자는 이 버튼을 보지 않는다(2.2 권한 고정과 정합).
- **승인 기반 원칙:** 요청은 즉시 실행되지 않는다 — **주최자 승인을 거쳐야** 실제 강등이 일어난다. 본인이 스스로를 강등시킬 수 있게 하면 필수 인원 정족수를 참석자 임의로 깨뜨릴 수 있게 되어 "누가 필수인지는 회의를 여는 사람이 정한다"는 2.2 권한 모델과 충돌한다.
- **진입 시 사유 입력 (v2.5, 발견 76):** 요청 확인 영역에 "요청 사유 (선택) — 주최자님이 승인 여부를 판단하는 데 도움이 돼요" 텍스트 입력을 포함한다. 강등 쪽의 의견 남기기와 대칭.
- **주최자 측 노출:** 대시보드 상단 통합 행동 배너(3.9)에 [승인]/[거절] 액션 포함. **가시성 요구사항 (v2.5, 발견 76):** 요청자가 입력한 사유가 이 배너에 그대로 노출되어야 한다 ("{이름}님이 참조자 전환을 요청했어요 — 사유: {사유}") — 사유 없이 승인/거절 버튼만 뜨는 것은 결함이다. 승인 시 기존 강등 경로(위 2번)와 동일하게 처리한다.
- **요청자 재진입:** 대기 중에는 "참조자 전환을 요청했어요. 주최자님의 확인을 기다리는 중이에요" 배너 + [요청 취소] 동선.
- **거절 시 통지 (v2.6, 발견 80):** 주최자가 거절하면 요청은 배열에서 조용히 사라지는 것으로 끝나지 않는다 — 요청자가 "대기 중" 배너가 사라진 것만으로 간접 추측하게 하는 것은 2.7 통지 원칙 위반이다. 요청자 화면에 명시적으로 **"요청이 거절됐어요"** 배너를 띄운다. 승인 시 강등 경로로 넘어가 그쪽 가시성 요건(위 항목)이 적용되는 것과 대칭으로, 거절도 결과가 눈에 보여야 한다.
- **확정 시 자동 폐기 규칙:** 주최자가 대기 중인 요청을 승인/거절하지 않은 채로 회의를 확정(COMPLETED)하면, 그 요청은 **자동으로 폐기**된다 — 확정된 회의에 대기 상태로 남는 요청은 의미가 없다. 재진입 시 잔존 요청 상태 없이 일반 확정 결과 안내만 노출한다(2.5-⑤).

**필수 복귀 요청 `[PRD-REINSTATE-REQUEST]` (v2.6 신설, 발견 81):** 역강등 요청이 "필수 → 선택"의 단방향이었던 것의 대칭 경로다. 강등됐던 사람이 나중에 시간이 다시 되면 "다시 필수로 넣어주세요"라고 요청할 방법이 지금까지 없었다.
- **진입:** 선택 참석자에게만 [다시 필수 참석자가 되고 싶어요] 요청 버튼을 노출한다. 필수 참석자는 이 버튼을 보지 않는다 — 이미 필수이므로 대상이 아니다.
- **승인 기반 원칙:** 역강등 요청과 동일하게, 요청은 즉시 실행되지 않고 **주최자 승인**을 거쳐야 실제로 attendance가 REQUIRED로 되돌아간다. 누가 필수인지는 회의를 여는 사람이 정한다는 2.2 권한 모델은 이 경로에도 동일하게 적용된다 — 참석자 스스로 필수 정족수를 임의로 늘릴 수 없다.
- **주최자 측 노출:** 역강등 요청과 같은 통합 행동 배너(3.9)에서 승인/거절 액션을 제공한다. 요청 유형(참조자 전환 요청 vs. 필수 복귀 요청)을 배너 문구에서 구분한다.
- **요청자 재진입:** 대기 중에는 "다시 필수 참석자가 되고 싶다고 요청했어요. 주최자님의 확인을 기다리는 중이에요" 배너 + [요청 취소] 동선. 거절 시 발견 80과 동일한 원칙으로 "요청이 거절됐어요" 배너를 띄운다.
- **확정 시 자동 폐기 규칙:** 역강등 요청과 동일 — 확정(COMPLETED) 시점까지 미처리된 요청은 자동 폐기된다.

### 5.2 [PRD-EX-02] Context: 이동 시간 누락 방지 정책

- **발생 상황:** 외부 미팅·출장 등 물리적 이동이 필요한 일정 직후에 조율 회의가 잡혀 참석이 불가능해지는 현상 방지.
- **해결 규칙:** 캘린더 데이터 파싱 시(데모에서는 시드 생성 시점 — 1.1) 일정 명칭에 `[외근, 출장, 미팅, 오프라인]` 키워드가 포함된 경우 해당 일정의 **시작 전 30분 및 종료 후 30분**을 `BLOCK_STRICT`로 강제 마스킹하여 유저가 선택할 수 없도록 차단한다.

### 5.3 [PRD-EX-03] UX Shock: 필수 참석자 미응답 자동 마감 정책

- **발생 상황:** 마감 시한 도달 시점(판정 기준: **가상 시계 — 1.3**)에 의사결정자 중 `PENDING` 상태인 멤버가 존재하는 경우.
- **해결 규칙:** 마감이 도달해도 결과 산출 화면으로 강제 전환하지 않고 마감을 일시 유예한다. 주최자 대시보드 상단에 미응답자 명단과 경고 배너를 활성화하며, 주최자에게 **'독촉 알림 전송'**(발송 연출 — 1.1) 또는 **'해당 인원을 제외하고 강제 마감 결과 보기'** 중 하나의 액션을 수동 선택하도록 강제한다.
- **프로액티브 재요청 `[PRD-PROACTIVE]` (v2.3 신설):** 눈치는 비선호를 입력하는 순간이 아니라 그게 그룹 앞에 실명으로 공개되는 순간 생긴다. 지금까지의 재요청(5.1)은 병목이 이미 대시보드에 실명으로 노출된 **사후** 대응이었다 — 이를 제출 시점으로 앞당겨 애초에 공개적 노출 자체를 예방한다. 그리드 제출 직후, 그 응답을 반영했을 때 "이 사람 한 명만 열면 완전 합의가 가능한" 상황이 되면, **제출한 본인에게만** 조용히 물어본다: "이 응답대로면 {슬롯}만 빼고 전원 합의가 가능해요. 혹시 그 시간도 열어주실 수 있나요?" 열어주면 병목이 대시보드에 실명으로 뜨는 순간 자체가 발생하지 않는다. 거절해도 정상 제출 완료로 진행되며, 이 질문에 응답하지 않은 것이 불이익으로 이어지지 않는다.
- **판정 보완:** 가상 시계가 `마감 전`이면 미응답자가 있어도 배너를 노출하지 않는다. 마감 전 대시보드는 "응답 현황(n/6 완료)"만 표기한다. 판정 대상은 의사결정자만이다 (3.7).
- **지각 제출 규칙 (v1.4):** 강제 마감으로 연산에서 제외된 미응답자가 이후 가용성을 제출하면, 해당 멤버는 자동으로 연산에 재포함된다 (별도 액션 불요). 강제 마감은 "제출 데이터가 없는 인원의 배제"이지 영구 제명이 아니다.
- **확인·통지 규칙 (v1.9 — 2.7 적용):** [제외하고 강제 마감]은 확인 영역(3.4 패턴)을 거친다. 영역에는 **독촉 발송 이력을 표기**한다 — 미발송이면 "{이름}님에게 아직 독촉을 보내지 않았습니다" + [먼저 독촉 보내기] 보조 버튼, 발송했으면 "독촉 발송함 ✓ · 응답 없음". 독촉 없이 마감할 자유는 주최자에게 있다 — 차단이 아니라 인지의 강제다. 독촉 발송 기록은 커밋된다(⑧).
- **사후 가시성 (2.7-③):** 제외된 본인이 PROGRESS 중 링크로 진입하면 배너로 제외 사실과 지각 제출 경로를 안내한다(2.5-⑤). 주최자 현황에는 "제외" 표기.

### 5.4 [PRD-EX-04] Concurrency: 최종 확정 순간의 동시성 제어 정책

- **발생 상황:** 주최자가 [최종 확정]을 누르는 순간과 데이터 커밋 사이에 타 참석자 캘린더에 신규 일정이 등록되어 더블 부킹이 발생하는 상황 방지.
- **해결 규칙:** 확정 즉시 커밋하지 않고 약 0.8초간 전원 가용성을 재검증하는 '동시성 싱크 체크' 상태를 거친다. 충돌 감지 시 확정을 취소하고 **대시보드의 조율 데이터를 갱신**하며 "그사이 팀원의 일정에 변동이 생겼습니다" 알림(지속 배너 — 1.5)을 노출한다.
- **실데이터 변동 규칙:** "갱신"은 문구 연출이 아니라 실제 데이터 변동이어야 한다 (1.1 원칙). 충돌 발동 시 시스템은 다음을 실제로 수행한다:
  1. 확정을 시도한 슬롯에 대해 **해당 슬롯에 가용한 의사결정자 응답자 1인(기본 박기획 m4, m4가 부적격이면 첫 번째 적격자)의 가용성을 `BLOCK_STRICT`로 변경**한다 (v1.9 — 강등 등으로 m4가 참조자가 된 경우에도 "추천이 제외되었다"는 배너가 거짓이 되지 않도록) — "그사이 외부 일정이 등록됨"의 재현. 변경은 localStorage에 커밋한다 (초기화로만 복원 — 7.4).
  2. 변경된 데이터로 추천 연산을 재실행한다 — Top 3 목록이 실제로 달라져야 한다 (확정 시도 슬롯은 추천에서 사라짐).
  3. 배너 문구에 변동 주체와 **제외된 슬롯을 특정**한다(1.7): "그사이 박기획님의 일정에 변동이 생겨 {슬롯} 추천이 제외되었습니다. 다른 시간으로 다시 확정할 수 있습니다."
  4. 갱신으로 새로 진입한 추천 카드에는 신규 표시를 부착한다 — 목록이 조용히 바뀌어 변화를 인지할 수 없는 상태를 금지한다(1.7).
- **v1.1 재정의:** "새로고침"은 **화면 전체 리로드가 아니라 대시보드 데이터 갱신**을 의미한다. 전체 리로드는 임시 상태를 파괴하고 첫 화면으로 리셋시키므로 금지한다.
- **발동 조건:** 시나리오 패널의 EX-04 토글이 ON일 때만 충돌이 감지된다. OFF(기본값)일 때 싱크 체크는 통과하고 확정은 정상 성공하여 `COMPLETED`에 도달한다. **무조건 충돌 처리 금지.** 충돌 1회 발동 후에도 토글이 ON이면 재확정 시 다시 발동한다.
- **상태 갱신 보장 (v2.5, 발견 75):** 매 확정 시도마다 상태 갱신은 **반드시 새 객체 참조로 커밋**되어야 한다. "대상자가 이미 BLOCK_STRICT라서 변경할 게 없다"는 이유로 이전 상태 객체를 그대로 재사용하면, 변경 감지 프레임워크가 "달라진 게 없다"고 판단해 재계산이 누락될 수 있다 — 이는 실제로 발생했던 결함이다. 값이 이미 같더라도 매번 새 객체를 생성해 커밋하고 재연산을 트리거해야 한다.

### 5.5 [PRD-EX-05] Post-Action: 확정 후 일정 변동 예외 정책 (v1.9 이원화)

- **발생 상황:** 확정 완료(`COMPLETED`) 이후 회의 성립 조건이 무너지는 경우. 발생 경로는 2개이며, **경로 A는 실구현, 경로 B만 모킹**이다 (1.1).

**경로 A — 참석 취소 선언 (v2.0 전면 재설계 — 발견 38·39):** 본인이 "이 시간은 안 된다"고 직접 말한 것은 "이 회의에서 완전히 빠지겠다"는 뜻이 아니다 — 재조율에도 계속 참여해 다른 시간을 다시 골라야 정상이다. v1.9의 "연산 완전 제외" 모델은 폐기한다.

  1. 진입: `COMPLETED` 확정 안내 화면(A01) 하단 링크 **[갑자기 참석이 어려워졌나요?]**.
  2. 본인 확인: 멤버 선택 (모킹 인증 — 1.1).
  3. 역할 분기 — 확인 영역(3.4 패턴):
     - **필수 참석자:** "회원님은 필수 참석자입니다. 참석을 취소하면 이 시간의 확정이 취소되고, 주최자에게 알림이 전송되며 일정 재조율이 시작됩니다." + [돌아가기] / [참석 취소하기] → 실행 시:
       - 확정됐던 슬롯 하나만 본인 가용성에서 `UNAVAILABLE`로 마킹한다(사유: "이전 확정 시간 취소"). **연산에서 본인을 완전히 빼지 않는다** — 재조율은 본인 포함 전원 기준으로 재연산된다 (`[PRD-CANCEL-SCOPE]` 신설).
       - `CONFLICT` 전이 커밋(③) + 주최자 지속 배너(실명).
     - **선택 참석자:** "회원님은 선택 참석자로, 회의 성립에는 영향이 없습니다. 주최자에게 불참 알림만 전송됩니다." + [돌아가기] / [불참 알리기] → 불참 기록 커밋(⑨). 상태 전이 없음.
  4. 사후 가시성 (2.7-③): 취소한 본인이 재진입해도 **다른 참석자와 동일하게 [내 가용 시간 업데이트하기]가 제공된다** — "무동선 안내"는 이 경로에 더 이상 적용하지 않는다 (2.5-⑤ 개정, 아래).
  - **`[PRD-CANCEL-SCOPE]` 원칙:** 재조율 재연산 결과와 완료 화면의 불참 표기는 반드시 같은 데이터(그 시점의 실제 가용성)를 참조해야 한다. "이탈자이므로 불참"이라는 고정 딱지를 재연산 결과와 무관하게 표기하는 구현은 결함이다.

**경로 C — 뒤늦은 제출 (v2.2 신설, 발견 60) `[PRD-LATE-JOIN]`:** 강제 마감으로 배제됐던 미확인자가 COMPLETED 이후 재진입해 처음 응답하는 경우다. 이 사람은 "취소"한 적이 없으므로 경로 A(참석 취소)로 안내하면 안 된다 — 별개의 사건이다.
  1. 진입: 확정 안내 화면의 [내 참석 상황을 확인하거나 바꾸고 싶어요] → 본인 선택. 목록에서 미확인자는 "아직 답 안 함 · 지금 제출"로 표시되어 경로 A 대상과 구분된다.
  2. 그리드 제출 → 제출한 가용성에 확정 슬롯이 포함되는지로 분기:
     - **포함(가능/비선호):** 상태 전이 없이 조용히 기록만 한다 — "이 시간에 참석 가능하신 걸로 기록했어요".
     - **불포함:** 이제서야 정당하게 재조율이 필요하다 — `CONFLICT` 전이, `dropReason: "LATE_MISMATCH"`. 주최자 배너와 본인 재진입 안내 모두 "취소했다"가 아니라 **"응답했는데 시간이 안 맞다"**는 사실을 그대로 말한다.
  3. `LATE_MISMATCH`는 연산 취급상 `SELF_CANCEL`과 동일하다 — 본인이 연산에서 완전히 빠지지 않고 재조율에 계속 참여한다(`[PRD-CANCEL-SCOPE]` 적용).

**경로 B — 외부 캘린더 변동 감지 (웹훅 모킹, v2.8 개정):** 시나리오 패널의 [이탈 발생시키기] 단발 버튼(1.4). `COMPLETED`에서만 활성, 비활성 시 사유 표기, 실행 즉시 소비 — 무한 루프 구조적 불가. 이탈 멤버는 최개발(m3) 고정. 제품 내 경로 A와 병존하는 것은 실제 제품의 구조(본인 선언 + 캘린더 연동 감지)와 동일하다.

  - **v1.9~v2.7 폐기 모델(발견 87):** 이전엔 "웹훅 감지 = 본인 연락 불가 상태"로 간주해 이탈자를 연산·갱신·재편집 동선 전부에서 완전히 제외했다. 이 가정은 틀렸다 — 최개발이 필수 참석자인 이상, 감지 경로가 자동이든 자진 신고든 그 사람 없이 회의가 조용히 재확정되는 것은 2.7 COMPLETED 불변식·필수 인원 배제 3요소 원칙 위반이다.
  - **v2.8 개정 모델:** 경로 B도 경로 A·C와 동일하게 **연산에서 본인을 완전히 빼지 않는다** — 재조율은 본인 포함 전원 기준으로 재연산되고(`[PRD-CANCEL-SCOPE]` 적용 대상 확장), 본인 재진입 시에도 다른 참석자와 동일하게 [내 가용 시간 업데이트하기]가 제공된다(2.5-⑤ 개정).
  - **경로 A와의 유일한 차이:** 경로 A는 본인이 직접 "취소"를 선언하는 행위 자체가 있으므로 그 즉시 확정 슬롯을 본인 가용성에서 `UNAVAILABLE`로 마킹한다(5.5 위 내용). 경로 B는 본인의 행위가 아니라 시스템 감지이므로, 이탈 발생 시점에 시스템이 대신 확정됐던 슬롯을 본인 가용성에서 자동 마킹한다(사유: "외부 일정 변동 감지", 상태값은 5.4의 `BLOCK_STRICT`와 동일 취급) — 이 마킹이 없으면 같은 슬롯이 재조율에서 그대로 다시 추천되는 모순이 생긴다(발견 87).
  - **경로 A와의 또 다른 차이 — 갱신 인정 시점:** 경로 A·C는 이탈을 유발한 본인의 그 행위(취소 선언, 뒤늦은 제출) 자체가 이미 갱신 신호이므로 재조율 반영 현황(6-2A)에서 즉시 "✓ 갱신함"으로 인정된다(6-2B). 경로 B는 본인이 아무 행위도 하지 않은 채로 시스템이 상태를 바꾼 것이므로, 슬롯 자동 마킹과는 별개로 **본인이 실제로 그리드에 재진입해 시간을 다시 알려야 "갱신함"으로 인정된다** — 자동 마킹을 본인의 갱신 행위로 대체하지 않는다.

- **공통 해결 규칙:** `CONFLICT` 전이 즉시 주최자에게 지속 배너(1.5)를 노출하고 주최자 화면은 재조율 플로우로 라우팅한다(2.6). 이탈자의 복귀 선언(취소의 취소)은 스코프 밖(8).

### 5.6 [PRD-CANCEL-MEETING] 회의 폐기 정책 (v2.3 신설)

- **발생 상황:** 시간 조율 문제가 아니라 회의 자체가 더 이상 필요 없어진 경우(안건 소멸, 프로젝트 무산 등). 이는 참석 취소(5.5)·재조율(6)과 근본적으로 다른 사건이다 — 부분적 배제가 아니라 **전체 종료**다.
- **발동 주체:** 주최자만. `PROGRESS`·`COMPLETED`·`CONFLICT` 어느 상태에서든 대시보드에서 [이 회의 취소하기]를 실행할 수 있다.
- **확인 영역 (3.4 패턴):** 결과 문장 "이 회의를 취소합니다. 모든 참석자가 더 이상 이 링크로 일정을 조율하거나 확인할 수 없게 됩니다." + 선택 입력 "취소 사유 (참석자에게 표시됩니다, 선택)" + [취소] / [회의 취소하기].
- **전이:** 즉시 `CANCELLED`로 전이하고 커밋한다. 상태 머신(4)의 유일한 흡수 상태 — 이후 어떤 액션으로도 되돌릴 수 없다.
- **가시성:** 전원이 재진입 시 동일한 읽기 전용 종료 화면을 본다(2.5-⑤, 2.6). 사유가 입력됐으면 함께 표시한다. 재제출·재조율 동선은 일절 제공하지 않는다.
- **불변식과의 관계:** CANCELLED는 2.7 배제 통합 원칙(선언·통지·가시성 3요소)이 적용되는 "일부 배제" 사건이 아니라 전원에게 동일하게 적용되는 종료이므로 별도 원칙으로 다룬다.

---

## 6. 재조율 우회 플로우 `[PRD-REMATCH]`

`CONFLICT` 상태 진입 시의 전용 플로우.

1. 기존 조율 데이터(전원의 제출된 가용성)는 폐기하지 않고 그대로 유지한다.
2. 이탈 멤버를 포함한 전원 기준으로 추천 연산(섹션 3.4)을 재실행하여 대체안을 제시한다(v2.8 개정 — 이전엔 경로 B에 한해 이탈 멤버를 완전히 제외했으나, 필수 참석자를 계산에서 조용히 빼는 것은 2.7 불변식 위반이므로 폐기했다). 이탈 멤버의 확정 슬롯 자동 마킹(경로 B — 5.5) 또는 취소 마킹(경로 A — 5.5)이 이미 가용성 데이터에 반영되어 있으므로, 별도 제외 없이 3.4 표준 연산만으로 그 슬롯이 자연히 후보에서 빠진다.
2-A. **재조율 반영 현황 (v2.6 신설, 발견 83):** PROGRESS 국면에는 "제출 현황 n/전원" 카드가 있어 누가 응답했는지 주최자가 한눈에 보지만, CONFLICT 국면엔 대응물이 없었다 — 이탈 시점 이후 각자가 가용성을 실제로 갱신했는지 주최자가 확인할 방법이 없어 재조율 최종 확정을 언제 눌러야 할지 판단 근거가 부족했다. R01에도 3.8과 대칭되는 현황 카드를 신설한다: 이탈(CONFLICT 진입) 시점 이후 가용성을 갱신한 인원과 아직 갱신하지 않은 인원을 구분 표시한다. 이 판정은 "최초 제출 여부"가 아니라 **"이탈 시점 이후의 갱신 여부"**이므로, 기존 멤버 상태(SUBMITTED, 최초 제출 이후 영구 유지)와는 별도의 추적 근거가 필요하다.

2-B. **최종 확정 차단 — 게이팅 (v2.7 신설, 발견 84):** v2.6까지는 반영 현황이 표시만 될 뿐, 실제 확정 가능 여부에는 영향을 주지 않았다 — 아무도 새로 갱신하지 않아도 옛 데이터로 산출된 대체안을 그대로 확정할 수 있었다. 이는 표시의 존재 목적 자체를 무력화한다: "지금 확정해도 되는지" 판단하라고 현황을 보여주면서, 정작 확정 액션 자체는 그 현황과 무관하게 항상 열려 있었다.
   - **차단 대상:** 2.6 재계산 대상에 포함되는 필수 참석자(경로 A·C처럼 계속 참여하는 경우) 전원이 이탈 이후 갱신을 마치기 전에는, R01의 [최종 확정] 액션을 노출하지 않는다. 3.7(조기 확정)·5.3(강제 마감)과 마찬가지로 **필수 참석자 기준**이며, 선택 참석자의 미갱신은 차단 사유가 아니다.
   - **경로 A(본인 취소)·경로 C(뒤늦은 제출 불일치) 예외:** 이탈을 유발한 본인의 그 행위 자체 — 경로 A는 해당 슬롯을 UNAVAILABLE로 마킹한 취소 선언, 경로 C는 뒤늦게 제출했으나 확정 시간과 안 맞은 그 제출 — 가 이미 갱신 신호다. 그 직후 별도로 그리드에 다시 들어가 "저 갱신했어요"를 증명하도록 요구하지 않는다. 이탈을 유발한 본인은 이탈 선언과 동시에 반영 완료로 간주한다.
   - **경로 B(웹훅) — v2.8 개정, 예외 폐지:** 이전엔 경로 B 이탈자를 차단 대상 필수 인원 집합에서도 제외했으나(연산 완전 제외 모델과 짝을 이루던 규정), v2.8부터 경로 B도 다른 필수 참석자와 동일하게 차단 대상에 포함된다. 단 경로 A·C와 달리 **자동 슬롯 마킹(5.5)은 본인의 갱신 행위를 대신하지 않는다** — 본인이 없는 채로 시스템이 상태만 바꾼 것이지 본인이 무언가를 "선언"한 게 아니므로, 반영 현황에서 "✓ 갱신함"으로 인정되려면 본인이 실제로 그리드에 재진입해 시간을 다시 제출해야 한다.
   - **화면 문구:** 필수 인원이 아직 남아 있으면 [최종 확정] 버튼 자리에 "모든 필수 참석자가 갱신하면 최종 확정할 수 있어요" 안내를 대신 노출한다(3.7의 "모든 필수 참석자가 응답하면 추천 시간이 산출됩니다"와 동일한 어조). 별도의 강제 확정(우회) 경로는 이번 범위에서 두지 않는다 — 재조율은 응답 마감 같은 시한이 없는 국면이라 5.3의 강제 마감과 성격이 다르다.

2-C. **CONFLICT 통합 플로우 (v3.0 신설, 발견 89):** 아래 두 규칙은 호스트/일반 참석자, 이탈 경로(A/B/C) 무관하게 CONFLICT 상태의 모든 참석자에게 동일하게 적용된다 — 2.5-⑤의 "주최자=참석자" 원칙, 5.5의 경로 통합 원칙과 같은 근거다.
   - **취소=대안선택 강제:** 경로 A(참석 취소)의 확인 액션 직후, "다른 시간을 찾고 있어요" 대기 화면을 거치지 않고 **곧바로 아래 [갱신 경로 판정]으로 강제 진입**한다 — 취소만 하고 아무 시간도 알리지 않은 채로 끝나는 상태를 허용하지 않는다. 이 강제 진입의 뒤로가기는 일반적인 "본인 재선택" 화면으로 돌아가지 않고, **취소 자체를 취소**한다 — 확정 상태·확정 슬롯 가용성을 취소 직전으로 그대로 복원한다. 최종 선택(대안 시간 제출 또는 원클릭 재확인)이 완료된 뒤에는 이 되돌리기 동선이 사라진다.
   - **갱신 경로 판정 (원클릭 재확인 vs 그리드 재편집):** CONFLICT 진입 후 1순위 추천 슬롯이 존재하고 부분 성립(레벨 3)이 아니라면, 그 슬롯에 대해 자신의 기존 가용성이 이미 AVAILABLE 또는 AVOID로 표시돼 있는 사람은 그리드를 다시 채울 필요가 없다 — "이 시간 여전히 가능하신가요?"라는 원클릭 확인만으로 2-B의 갱신 인정(reMatchUpdatedIds 반영)을 받는다. 그렇지 않은 사람(해당 슬롯에 값이 없거나 UNAVAILABLE/BLOCK_STRICT인 사람, 또는 애초에 전원 커버되는 슬롯이 없는 경우)은 기존과 동일하게 그리드 전체를 다시 채운다. 원클릭 화면에는 "다른 시간을 직접 고를게요" 링크로 그리드 편집으로 전환하는 경로도 항상 열어둔다. **이미 반영을 마친 상태에서 같은 화면에 재진입하면(v2.5, 발견 90) 버튼은 "✓ 반영 완료" 라벨의 비활성 상태로 표시된다** — 이중 반영·중복 클릭을 막고, 확인이 실제로 기록됐다는 것을 시각적으로 보여준다.
   - **2-B 게이팅과의 관계 — 옛 데이터 재확정과 다른 이유:** v2.7(발견 84)이 막았던 것은 "아무도 명시적으로 갱신하지 않았는데 옛 데이터로 산출된 결과를 그냥 확정하는 것"이었다. 원클릭 재확인은 이와 다르다 — 시스템이 임의로 넘어가는 게 아니라, **그 사람에게 구체적인 후보 슬롯을 직접 보여주고 지금 이 순간 명시적인 예/아니오 신호를 받는다.** 게이팅이 요구하는 "명시적 갱신 신호"의 요건은 그대로 충족하며, 그 신호를 받는 절차만 가벼워진 것이다. 따라서 `reMatchUpdatedIds`가 실제 판정 기준이라는 6-2B의 원칙 자체는 바뀌지 않는다 — 어떤 예외도 추가하지 않는다.
3. 주최자가 대체안을 확정하면 상태는 다시 `COMPLETED`로 전이한다 (EX-04 검증 동일 적용, 전이는 커밋 — 7.2).
4. 대체안이 0개인 경우 EX-01 완화 정책(3단계 포함)이 동일하게 적용된다.
5. **가드 상태:** 회의 상태가 `CONFLICT`가 아닌 상황에서 R01에 진입한 경우(패널 바로가기 등), 재조율 UI를 렌더링하지 않고 "현재 재조율이 필요한 회의가 없습니다" 안내와 대시보드 복귀 수단만 노출한다. 이탈 멤버가 없는 상태에서 이탈 문구를 조립하는 것(미정의 상태 노출)을 금지한다.

---

## 7. 데이터 라이프사이클 및 영속성 정책 `[PRD-DATA-RULE]`

1. **임시 메모리 격리:** 참석자가 그리드에서 편집하는 동안의 데이터는 브라우저의 임시 상태값에만 저장되며, localStorage 쓰기 작업을 수행하지 않는다.
2. **영속성 커밋 시점 (v1.4 확대):** 아래 사건이 발생하는 순간마다 회의 데이터 전체를 직렬화하여 localStorage에 커밋한다. **커밋은 실제로 수행되어야 하며, 완료 연출로 대체하는 것을 금지한다** (1.1 원칙).
   - [가용성 최종 제출] — 임시 데이터 병합 + 멤버 `SUBMITTED` 전환
   - 회의 상태 전이 전부 — 확정(`COMPLETED`), 이탈 감지(`CONFLICT`), 재조율 확정(`COMPLETED`)
   - EX-04 충돌 발동 시의 가용성 변동 (5.4)
   - 발의 (launched 플래그 — 유입 트리거, 2.5-②) 및 권한 강등 (5.1-⑦)
   - 독촉·재요청 발송 기록 (5.3, 5.1 — ⑧) 및 참조자 불참 기록 (5.5-⑨)
   - 새로고침으로 위 사건의 결과가 되돌아가는 구현은 결함이다.
3. **초기 로드 규칙:** 앱 시작 시 localStorage에 커밋본이 존재하면 시드 데이터 대신 커밋본을 로드한다. 커밋본이 없으면 시드 데이터로 시작한다.
4. **초기화:** 시나리오 패널의 [데이터 초기화]와 완료 화면의 [새 회의 만들기](2.5-⑥)만이 커밋본을 삭제하고 시드 상태로 복원하는 경로다.
5. **가상 라우팅 언마운트 방어:** 화면 전환 시 편집 중이던 임시 데이터는 상위 콘텍스트에 격리 보관하여, 화면 복귀 시 그대로 유지되어야 한다 (00_alignment_guide 3항과 연동).
6. **멀티탭 동기화 (v1.4 신설) `[PRD-SYNC]`:** 같은 브라우저의 다른 탭/창에서 발생한 커밋(7.2)은 storage 이벤트 수신으로 **실시간 반영**한다 — 참석자 창에서 제출하면 주최자 창의 응답 현황이 즉시 갱신되어야 하며, 상태 전이 수신 시 진입 가드(2.6)를 즉시 적용한다. 이는 연출이 아닌 브라우저 네이티브 기능의 실구현이다. **경계:** ① 동기화 대상은 커밋된 회의 데이터만이다 — 시나리오 토글·가상 시계·편집 임시 버퍼는 탭 로컬(1.3, 1.4). ② 두 탭이 동시에 커밋하면 나중 쓰기가 이긴다(last-write-wins) — 서버 부재의 알려진 한계로, 충돌 해결은 스코프 밖(8). ③ 데모 가이드(1.6)에 두 창 사용법과 이 경계를 안내한다.

---

## 8. 스코프 노트 — 의도된 미구현 `[PRD-SCOPE-OUT]`

아래 항목은 QA·설계 검토에서 발견되었으나 **의도적으로 이번 버전에서 제외**한다. 누락이 아니라 결정이다.

| 항목 | 제외 사유 | 재검토 시점 |
|---|---|---|
| 회원가입·로그인 | 무가입 링크 진입이 제품 핵심 가치(2.1). 가입 플로우 추가는 정체성과 충돌 — 여정 시작점 결핍은 랜딩·초대 랜딩(2.5)으로 해결 | 실서비스 전환 시 주최자 계정에 한해 검토 |
| 회의 단위 불참 선언 (참석자가 "이 회의 자체에 참석 불가"를 선언) | 선언된 멤버의 연산 제외 여부, EX-01 완화 순서, EX-03 판정(불참 ≠ 미응답), 상태 머신까지 연쇄 영향을 주는 신규 기능. 완화 3단계의 "참석 불가 예상" 표기(3.4)가 결과 측면을 부분 대체 | 별도 사이클로 PRD 개정 후 진행 |
| 마감 이후 재편집 제한 | 데모 시연 가치 대비 규칙 복잡도 과다 (3.5 참조) | 실서비스 전환 검토 시 |
| 발의 화면의 기간·마감 편집 기능 | 데모 그리드·가상 시계와의 정합성 유지를 위해 표시형으로 제한 (2.2) | 실서비스 전환 시 |
| 발의 후 회의 정보 수정·회의 취소 | 수정은 그리드 범위·기제출 데이터 무효화 정책이 연쇄로 필요. 데모는 회의 1건 고정이므로 [새 회의 만들기] 순환(2.5-⑥)이 부분 대체 | 실서비스 전환 시 |
| 마감 전 상시 독촉 기능 | 독촉은 EX-03 유예 국면의 강제 선택지로만 제공 — 시나리오 시연 가치에 집중 | 실서비스 전환 시 |
| 브라우저 히스토리(뒤로가기) 연동 | 가상 라우팅 구조의 알려진 한계. 히스토리 연동은 진입 가드(2.6)·강제 라우팅과의 충돌 케이스를 양산 | 실서비스 전환 시 |
| 멀티탭 동시 편집의 충돌 해결 | 서버 부재로 last-write-wins만 가능(7.6). 데모 가이드에 한계 명시로 갈음 | 실서비스 전환 시 |
| 도메인 멤버 추가·삭제 | 멤버 목록은 디렉토리 연동 모킹(1.1) — 6인 고정이 과제 브리프 조건(2.4) | 실서비스 전환 시 |
| 강등 이력의 화면 표기 (변경 로그) | 강등 후 화면상 참조자와 동일 취급 — "원래 필수였다"는 이력은 감사 로그 영역 | 실서비스 전환 시 |
| 이탈자의 복귀 선언 (취소의 취소) | 재조율 진행 중 연산 재편입의 상태 머신 연쇄 과다 | 별도 사이클 |

---
